# Модель отклика серы на температуру ГО — версия T6/F9

Статус 2026-09-17. Пересчёт принятой спецификации (`RESPONSE_MODEL_FINAL.md`, версия 3) на официальные теги справочника
24-2000 от 16.09: вход реактора Р-202 — `ht.T6` (было `T11` = выход Р-202), расход сырья — `ht.F9` (масса, т/ч; было `F26` =
гидроочищенный ДТ в цех №8). Математика, окна, правило коэффициента, envelope и неопределённость — без изменений.
Код: `context/response-research/t6/` (`response_model.py`, `s1_frame.py`, `f1_*`, `f2_*`, `f6_*`, `f7_*`, `f9_fit.py`,
`compare_t11.py`, `c2_export.py`), журнал `context/response-research/t6/notes.md`. Контракт с дорожкой A — `config/response_model.json`.

> **Актуальные числа production-контура (артефакт обучения 19.09, `uv run neftecode train`, `artifacts/response_model.json`,
> τ = 2026-01-01):** β = **−0.4227** [−0.4768; −0.3890], weak/strong −0.211 / −0.721, n_rows 49 105,
> T6 342.9–386.1 °C, F9 149.8–256.7 т/ч, порог расхода q01(F9) = 133.457 т/ч, выученный на строках до τ − 6 ч.
> Отличие от таблицы ниже (−0.433): порог F9 в исследовательском прогоне 17.09 считался по всей истории
> (утечка закрыта позже), 18.09 исправлено усечение маски работы установки. Таблица ниже — исследовательский
> прогон 17.09, сохранён как история и описание метода.

Важно различать поля артефакта. В текущем `artifacts/response_model.json` верхнеуровневые `train_end`,
`tau` и `beta_mgkg_per_c` относятся к `train_end = 2025-01-01` (β = −0.4749), тогда как
`estimates` содержит более позднюю оценку τ = 2026-01-01 (β = −0.4227). Это не временная утечка:
`response_at()` выбирает последнюю оценку с τ ≤ времени решения (`src/neftecode/infrastructure/response/estimate.py:171–190`).
Числа во врезке — выбранная live-оценка для 2026, а не копия верхнеуровневого поля артефакта.

## 1. Результат исследовательского прогона 17.09 (τ = 2026-01-01, окно 12 мес.)

Числа этого раздела — **история метода, а не то, что считает продукт**. Действующие значения — во врезке выше
и в `artifacts/response_model.json`; продукт использует β = −0.4227.

| Величина | Значение (прогон 17.09, не действующее) |
|---|---|
| β (ARX-плато 3–8 ч на устойчивый шаг +1 °C по T6) | **−0.433 мг/кг/°C** |
| CI90 (бутстрап по месяцам, 40 повторов) | [−0.476; −0.397] — нуля не включает |
| n (10-мин строк ARX в окне 2025-01-01 … 2025-12-31 18:00) | 48 938 |
| weak / strong (0.5·β; max прошлых rolling-origin отношений = 1.71) | −0.217 / −0.739 (cap −1.0 не срабатывает) |
| w (вес ПАК-30 мин в baseline) | 0.10 |
| радиусы r0 / r1 (90 %-квантиль остатка, тихие / с действием) | 1.57 / 2.48 мг/кг |
| envelope | \|ΔT\| ≤ 2 °C за шаг; зона A при \|ΔT\| ≤ 1 и ≥ 5 аналогов; T6 в q01–q99 истории 342.9–386.1 °C; F9 150–257 т/ч |
| честный k для `ratio = exp(−k·ΔT)` при среднем ПАК 8.25 | 0.053 [0.048; 0.058] (в `baseline.json` назначено 0.085 — в 1.6× больше) |

Проверка на следующем полугодии (2026H1, n = 6967, действий 716): MAE all 1.053 (baseline 1.065), MAE action 1.483 (1.562);
покрытие all / quiet / action 0.825 / 0.822 / 0.853; наклон realized/predicted на действиях 0.84 [0.59; 1.46] — величина β
подтверждается (в T11-версии наклон был ≈ 2, эффект недооценивался вдвое). Покрытие тихих строк 2026 ниже 0.9 — как и раньше,
это baseline (ПАК стал волатильнее), не β.

Текущий артефакт на конец истории (τ = 2026-08-07): β = −0.495 [−0.538; −0.452], w 0.20, r0/r1 2.28/3.18.

## 2. Дрейф по полугодиям (тот же ARX внутри полугодия, realized)

| | 2023H1 | 2023H2 | 2024H1 | 2024H2 | 2025H1 | 2025H2 | 2026H1 |
|---|---|---|---|---|---|---|---|
| **T6/F9** (свои строки) | −0.285 | −0.260 | −0.373 | −0.507 | −0.461 | −0.491 | −0.467 |
| T11/F26 (final) | −0.13 | −0.11 | −0.20 | −0.34 | −0.31 | −0.53 | −0.65 |

На T6 дрейф ×1.7 за 3.5 года и плато с 2024H2 (−0.46…−0.51); на T11 был рост ×5. Rolling-origin для правила trailing-12:
отношение realized/estimate 1.34, 1.71, 0.97, 1.02, 1.08 (2024H1…2026H1), mean |log| 0.19 (T11: 0.45); на EVAL-полугодиях 0.05
(T11 0.49). Большая часть «дрейфа» T11-версии была дрейфом связи выход↔вход реактора, а не отклика серы.

## 3. Сравнение с T11-версией на ОДНИХ И ТЕХ ЖЕ строках (`compare_t11.json`)

Строки — пересечение stable-масок обоих правил (152 716). Четыре комбинации тегов разделяют вклад температуры и расхода.

| теги | β trailing-12 @2026-01-01 | @2026-08-07 | все данные | 2023H1 | 2024H2 | 2026H1 |
|---|---|---|---|---|---|---|
| T11 / F26 | −0.396 [−0.452; −0.358] | −0.594 | −0.294 | −0.127 | −0.356 | −0.652 |
| **T6 / F9** | **−0.468 [−0.504; −0.447]** | −0.550 | −0.413 | −0.290 | −0.528 | −0.592 |
| T6 / F26 | −0.468 | −0.550 | −0.413 | −0.290 | −0.527 | −0.591 |
| T11 / F9 | −0.396 | −0.594 | −0.295 | −0.128 | −0.357 | −0.653 |

- Тег расхода не влияет на β (< 0.002) — поэтому `flow_beta = null`: расход входит в ARX только как контроль, отдельный
  устойчивый эффект расхода не идентифицирован (F30 в DML: R² по состоянию ≈ 0, θ незначим).
- Тег температуры влияет: |β| по T6 в 2.3× больше в 2023 и ≈ равен T11 в 2026.
- Прочие оценки (SELECT, только до 2025-07-01): DML h3 per delivered °C −0.32 [−0.39; −0.26] (T11 −0.29); onset −0.38 (T11 −0.17);
  matching 687 пар −0.36 (T11 −0.50); ARX SELECT плато −0.37 (T11 −0.24). Лаг < 1 ч, плато с 3 ч — как раньше.

## 4. Почему T6-версия корректнее по смыслу

1. T6 — температура ГСС на входе Р-202, то есть то, что регулятор держит по уставке; T11 — выход Р-202, следствие режима
   (экзотерма, расход, активность катализатора). Шаг по T11 — это не действие оператора, а смесь действия и отклика.
2. По уровням T6 и T11 совпадают (r = 0.993), но по 10-мин приращениям r = 0.54: ARX на разностях видит разные входы,
   и именно на T11 эффект «размазан» и растёт со временем (выход всё сильнее отстаёт от входа), на T6 — стабилен.
3. Onset-подмножество (нет движения T и скачка ПАК перед t) на T6 даёт эффект не слабее pooled (−0.38 vs −0.32), на T11 —
   вдвое слабее: шаг по T6 меньше загрязнён обратной связью APC.
4. Наклон realized/predicted на действиях следующего полугодия ≈ 1 (0.84 [0.59; 1.46] в 2026H1, 1.26 в 2025H2) против ≈ 2 у T11.

## 5. Что осталось неопределённым

- CI в C2 — статистический (бутстрап по месяцам); реальная ошибка на следующем полугодии по rolling-origin до ×1.7 (2024H2),
  в 2025–2026 — ≤ 8 %. Правило weak/strong (0.5·β … 1.71·β) это покрывает; узкий CI сам по себе не гарантия.
- Чувствительность к правилу running в 2026H1: β −0.47 на новых строках против −0.59 на общих с T11 строках (разница —
  ≈ 2000 строк, выпадавших из старого правила из-за окна 12 ч/6 ч). Единственное место, где отбор строк заметно меняет β.
- Покрытие тихих строк в 2026 (0.82) ниже целевых 0.9 — проблема baseline, не отклика; при переносе в src интервал на 2026
  нужно расширять по последнему полугодию (r0 2.28 на τ = 2026-08-07).
- Идентифицировано на исторических шагах APC (|A30| ≥ 1 °C — 18 % строк), не на плановых экспериментах; «устойчивый ΔT»
  реализуется APC лишь на 0.62–0.66 от начального шага (first stage), ARX это учитывает через полный путь T6.
- F15 (объёмный расход сырья по справочнику) не согласуется с F9 ни по масштабу, ни по динамике — см.
  `context/response-research/t6/f15_scale.md`; в модели не используется.
- Аддитивная форма и постоянство эффекта по состоянию (F3 в T11-версии) на T6 повторно не проверялись — правило принято как есть.

## 7. Аудит production-кода (21.09.2026)

- `prepare()` обучает порог q01(F9) только на горячих строках не позже τ−6 ч; ARX-окно заканчивается
  там же (`src/neftecode/infrastructure/response/preparation.py:25–33`, `estimate.py:76–103`).
- Живой выбор оценки — latest `tau <= decision_time`, а не значение из верхнего уровня JSON
  (`estimate.py:171–190`).
- Формулировка «ARX по данным 2025 года» в `DataResponseEffect` устарела: эффект получает β из
  выбранной оценки по τ, поэтому в операторских документах не следует приписывать его одному году
  (`src/neftecode/infrastructure/response/data_model.py:35–41`).
- Результат описывает серу потока после ГО и не доказывает причинный промышленный эффект; модель
  ограничена `|ΔT| ≤ 2 °C` и историческим диапазоном T6/F9.

## 6. Файл контракта

С T112 (17.09) разделены политика и оценка. `config/response_model.json` (schema v1, в git) — только объявленная
политика: tag `ht.T6`, flow_tag `ht.F9`, window_months 12, envelope_dt_c 2.0, `response_onset_hours` 2,
`observed_plateau_window_hours` 3–8, `horizon_response_share` 0.66, flow_beta null. Оценки β по сетке τ
(1 января / 1 июля, плюс `train_end` и конец
данных) пишет `train` в `artifacts/response_model.json` тем же ARX (`src/neftecode/infrastructure/response/estimate.py`):
для каждого τ окно 12 мес. до τ − 6 ч, порог F9 = q01 по «горячим» строкам не позже τ − 6 ч, дрейф по полугодиям
на строках основной оценки (`train_end`), `model_fingerprint` из `artifacts/manifest.json`. Живое решение берёт
последнюю оценку с τ ≤ момент решения. Текущие числа для τ = 2026-01-01 — в примечании вверху файла.
Исследовательский экспорт `c2_export.py` (числа таблицы §1) сохранён как история и в production не используется.
