# CUTPOINT — перестройка фронтенда: схема расчёта

Третий круг. Заменяет вертикальную ленту из восьми цветных секций на **технологическую
схему расчёта**: узлы-этапы, соединённые стрелками, уложенные змейкой в три строки.
Нажатие «Пуск» запускает настоящий прогон; схема проигрывает его по мере прихода событий.
Любой узел раскрывается в панель с временем, характеристиками и всей работой этапа.

Источники направления: два референса руководителя (ряд карточек 01–06 со стрелками;
тёмная схема «контур данных / контур решения» из `context/data-flow.html`) и его требования:
не одна строка, а змейка; убрать разноцветные подложки; раскрывать этап; «интересная
и крутая визуальная штука», но здравый продукт без ИИ-слопа.

Контекст для скиллов: `PRODUCT.md` (регистр product), `DESIGN.md` (монохром, шрифт, радиусы),
`FRONTEND_TODO.md` (что уже сделано и что запрещено — п. 1–10 действуют без изменений).

---

## 0. Решения, принятые до начала работ

Каждое можно оспорить, но не молча: если агент отступает — пишет почему в отчёт.

### 0.1 Светлая тема остаётся основной

Референс руководителя тёмный, но это тёмный вариант `context/data-flow.html`, который
по умолчанию светлый и переключается системной темой. Основная сцена — проектор в комнате
защиты: тёмный фон на проекторе теряет контраст, светлый читается. Логотип, `DESIGN.md`
и вся система мер построены на vellum/carbon. Тёмная тема — отдельная финальная фаза Ф6
после показа руководителю, через `prefers-color-scheme` и явный переключатель;
токены для неё уже есть в `data-flow.html` (`--paper #11171A`, `--surface #182126`,
`--ink #E2E8E5`). **Это единственный пункт, который стоит подтвердить у руководителя
до Ф6; до этого он ничего не блокирует.**

### 0.2 Восемь подложек удаляются целиком

`--band-*`, `--band-line-*`, `--band-field-*`, `--band-hover-*`, файл `bands.css`,
атрибут `data-band` — всё вон. Иерархия строится на монохроме: vellum → paper → carbon,
рамка сплошная / пунктир / двойная, начертание 300/500/600.

### 0.3 Один акцент — янтарь логотипа

`--accent: #c8761a` (уже в `Logo.tsx`). Значение «сейчас»: рамка и номер текущего узла,
стрелка, которая сейчас рисуется, слой агентов (как жёлтый узел LLM у руководителя).
Контраст `#c8761a` на vellum ≈ 2,97 — **годится только для рамок ≥ 2px и заливок,
не для текста.** Для текста — `--accent-ink: #8a4d0f` (≈ 5,8 на vellum; пересчитать
в реализации, значения ориентировочные). Семантические `--pass / --fail / --unknown`
остаются только на состоянии, как сейчас.

### 0.4 Змейка настоящая, бустрофедон

Строка 1 слева направо, строка 2 справа налево, строка 3 слева направо. Стрелки короткие
и непрерывные, без длинной возвратной линии. Номера на карточках ведут глаз, поэтому
чтение «6 ← 5 ← 4» не путает. Если в аудите окажется, что путает, — запасной вариант:
все строки слева направо с угловым коннектором справа вниз-налево (как у руководителя),
цена — одна длинная линия на строку.

### 0.5 Схема показана до пуска

Все узлы в состоянии «ожидает» (пунктир, приглушённый текст) видны под формой условий
с первой секунды. Зритель видит маршрут до того, как нажал «Пуск». Это и есть
«одна схема всей системы» из `data-flow.html`.

### 0.6 Анимация — это реальный ход расчёта

Никакой отдельной «красивой прокрутки» с выдуманным темпом. Узлы загораются по событиям
сервера через существующую очередь раскрытия (`run/revealQueue.ts`). Выдержка между
шагами поднимается с 260 до **450 мс**, чтобы стрелка успевала прорисоваться (320 мс)
и узел — включиться (130 мс). Время на узлах остаётся измеренным (`<0,1` для этапов,
прошедших за миллисекунды) — темп показа и измеренное время различаются, и об этом
говорит существующая оговорка «раскрыт из payload — это не длительность расчёта».

Повтор («▶ ещё раз» после конца прогона) проигрывает **записанную** ленту событий
этого же прогона со сжатием пауз агентов; на экране подпись «повтор записи, паузы сжаты».

### 0.7 Что раскрывается, а что нет

| Узел | Раскрытие | Почему |
|---|---|---|
| 0 Условия | да, компактно: `ConfigBrief` + «Новый прогон» | форма до пуска, после — сводка |
| 1 Состояние | да | уставки, рецепт, запасы, происхождение |
| 2 Доверие к данным | да | источники, возраст замеров, вердикт |
| 3 Кандидаты | да | воронка отсева, раунды, семейства |
| 4 Прогноз | да | запасы по свойствам, траектория, упреждение |
| 5 Выбор | да | альтернативы, чем отличаются, устойчивость, offspec |
| 6 Gate | да | матрица 129 проверок, запас по уставкам |
| 7 Агенты | да | диалог, инструменты, бюджеты, вето, мнения |
| 8 Решение | **нет — всегда открыт** | это итог; постоянная панель «Итог» под схемой |
| Входы (сценарий, модель) | нет | структурная подпись, данных на раскрытие нет |
| Исходы hold / recommend / refuse | нет, клик = прокрутка к «Итогу» | это ответ, а не этап |

### 0.8 Левая рейка удаляется

Схема сама является навигатором. Вместо рейки — тонкая липкая полоса состояния,
появляющаяся, когда схема ушла за верх экрана: часы, текущий этап, «к схеме ↑», стоп.

### 0.9 Что нельзя потерять (перечень оговорок честности)

Все они существуют сейчас и обязаны существовать после перестройки, дословно или по смыслу:
подпись источника этапа (`stageSource`: отметка сервера / раскрыт из payload);
полоса провайдера «живая модель» / «детерминированная политика» / `fallback` с причиной;
бейджи происхождения чисел (`OriginBadge`); тексты `Empty` («не передавалось»);
подпись под `MarginBars` об отказе от «процента здоровья»; ступенчатая траектория;
«последний ответ сервера N с назад» при обрыве; «повтор записи, паузы сжаты».

---

## 1. Информационная архитектура схемы

### 1.1 Узлы

Три вида узлов. Только этапы (`stage`) имеют состояние и время; остальные статичны.

```
kind: stage    — 8 этапов сервера + узел 0 (условия); имеют pending/running/done/failed
kind: input    — внешний вход, пунктирная рамка, без состояния:
                 «Сценарий и срез» (имя сценария, момент, отказ источника — из условий)
                 Узел «Модель прогноза» ОТМЕНЁН: проверено, сервер не шлёт ни имени модели,
                 ни метрик (`Forecast` несёт только траекторию свойства; в `ui.py` полей о
                 модели нет). Узел мог бы нести только статичный текст из README, не связанный
                 с прогоном, — это запрещено п. 2 и п. 10. Вход остаётся один.
kind: terminal — три исхода: hold · recommend_scenario · refuse; загорается один
```

Раскладка на ≥ 1100 px — четыре колонки, три строки, змейка:

```
КОНТУР ДАННЫХ     [вход: сценарий и срез] → [1 Состояние] → [2 Доверие к данным] → [3 Кандидаты] ┐
                                                   ↓ выход ОТКАЗ (нет годного источника)         │
КОНТУР РЕШЕНИЯ    [6 Gate] ← [5 Выбор] ← [4 Прогноз: упреждение 48 ч] ←───────────────────────────┘
                     │  ↑ пунктир: агенты сужают выбор → снова Gate       ↺ у 3: вето сужает поиск, до 3 раундов
СЛОЙ АГЕНТОВ      [7 Агенты · LLM] → [8 Решение] → { hold | recommend_scenario | refuse }
```

Порядок этапов и их смысл соответствуют серверу: прогноз-упреждение считается **до**
выбора и может заменить план (`_look_ahead`, `switched`), поэтому он четвёртый.
Подписи контуров слева от строк — как у руководителя, мелкими капителями с квадратом-маркером
(уже есть `.label` в `stage.css`).

### 1.2 Рёбра

```
flow  — основной ход, сплошная линия со стрелкой; рисуется при переходе к следующему узлу
loop  — петля 3→3 «вето сужает поиск»; подпись становится «раунд N из 3», когда rounds > 1
back  — пунктир 7→6 «сужает выбор → снова Gate»; загорается, когда в трассе есть replans > 0
exit  — пунктир к refuse из 2, 3, 6, 7; всегда виден бледным (это документация системы),
        красным — только если отказ действительно случился на этом узле
```

Таблица выхода по `payload.decision.refusal.kind` (виды взяты из `make_decision.py`,
`decision/lookahead.py`, `agentic/decision.py`):

| kind | узел выхода |
|---|---|
| `data` | 2 Доверие к данным |
| `no_feasible_plan`, `computation_error` | 3 Кандидаты |
| `final_recheck_failed` | 6 Gate |
| `agent_rejected`, `weak_response_failed` | 7 Агенты |
| неизвестный | 8 Решение — не гадать, выход из итога |

Обрыв потока / ошибка сервера (`status: failed`) — красная двойная рамка на реально
шедшем узле (логика уже есть в `useRun.fail`), стрелки дальше не рисуются.

### 1.3 Карточка узла-этапа

Фиксированный размер (около 250 × 128 px на четырёх колонках), содержимое не меняет
высоту — схема не должна дёргаться. Сверху вниз:

```
03                      ← номер, моно, 22px; у текущего — акцент
Кандидаты               ← заголовок, 16px, 500
optimizer · 3 раунда    ← моно-подпись 12px, приглушённая: артефакт этапа (как у руководителя)
                          до пуска — что этап сделает; после — 1–2 главных факта из StageFacts
0,6                     ← время справа внизу, моно; у текущего — живые часы (useLiveClock)
```

Факты берутся из существующего `run/stageFacts.ts` (первые две строки `*Lines`),
а для 7 Агентов — из `agentMeters`/`orchSteps`: «ход 2 из 5 · ждём агента качества 12,3».
Это то самое «видно, что оркестратор решает», без раскрытия карточки.

Состояния карточки (цвет никогда не единственный признак):

| состояние | рамка | номер | слово |
|---|---|---|---|
| pending | 1px пунктир `--line-soft` | приглушён, 400 | ожидает |
| running | 2px сплошная `--accent`, верхняя линия 3px | акцент, 600, мигает `rail-blink` | идёт |
| done | 1px сплошная `--carbon` | `--pass`, 700 | готово / без отметок |
| failed | 3px двойная `--fail` | `--fail`, подчёркнут | отказ |

Единственная бесконечная анимация во всём проекте — мигание номера текущего узла
`steps(1)` 1200 мс (уже принято пользователем). Без ореолов и пульсаций рамки.

### 1.4 Панель раскрытия

Клик по узлу (кнопка `aria-expanded`) открывает панель **под строкой этого узла** на всю
ширину схемы; открыта одна панель. Заголовок панели:

```
3 · Кандидаты — готово · начат на 0,17 с · длился 0,6 с · отметка сервера        [закрыть ×]
```

Тело — существующие компоненты этапов без своей шапки (`Section` перестаёт быть
обёрткой; `stages/*Stage.tsx` возвращают тело). До прихода `payload` тело панели —
`StageLive` + `AgentDialogue` (то, что сейчас в `StageOutline`). Ничего из
визуализаций второго круга не переписывается — переезжает целиком.

Открытие: `grid-template-rows: 0fr → 1fr`, 240 мс, `--ease`; reduced-motion — мгновенно.
Схема под панелью пересчитывает стрелки (ResizeObserver на контейнере).

### 1.5 «Итог»

После события `screen` под схемой появляется постоянная панель «Итог» — тело
`DecisionStage` (вердикт, действия, отказ, объяснение). Это кульминация страницы,
поэтому она не прячется в раскрытие. Клик по исходу-терминалу прокручивает к ней.

### 1.6 До пуска и после

До пуска: логотип → форма «Условия прогона» (с кастомными селектами, как сейчас) →
кнопка «▶ Пуск» → схема в ожидании. После нажатия: форма сворачивается в узел 0
(240 мс, содержимое заменяется сводкой), схема поднимается, начинается прогон.
«Стоп» и Escape — как сейчас. После конца: «▶ Ещё раз» (повтор записи) и «Новый прогон».

### 1.7 Отзывчивость

- ≥ 1100 px — четыре колонки, три строки змейкой.
- 720–1100 px — две колонки, змейка в пять строк; подписи контуров над строками.
- < 720 px — одна колонка, вертикальный ход сверху вниз, стрелки вертикальные,
  петли и выходы — короткие пунктиры сбоку с подписью. Панели раскрытия — под узлом.
- Печать — статичная схема с состояниями через стили рамок, все панели раскрыты,
  стрелки чёрные, акцент → жирная рамка (принцип `print.css` сохранён).

---

## 2. Техническое устройство

Новые файлы (каждый ≤ 250 строк, ноль комментариев, именованные экспорты):

```
src/frontend/src/map/
  graph.ts          узлы и рёбра как данные: id, kind, строка/колонка на 4-2-1 колонках,
                    подписи, артефакт, тексты ожидания, выходы
  layout.ts         число колонок → змейка: порядок, направление строки, координаты в сетке
  useNodeRects.ts   ResizeObserver + rAF → карта прямоугольников узлов относительно схемы
  wires.ts          ортогональные SVG-пути по прямоугольникам: flow/loop/back/exit,
                    точки входа-выхода с учётом направления строки
  WireLayer.tsx     SVG-оверлей поверх схемы, aria-hidden; рисование stroke-dashoffset
                    один раз при смене состояния ребра; подписи рёбер как <text>
  PipelineMap.tsx   контейнер: строки, узлы, оверлей, слот панели
  StageNode.tsx     карточка этапа (button), состояния, факты, время
  InputNode.tsx     пунктирный вход
  TerminalNode.tsx  три исхода
  Drawer.tsx        панель раскрытия: заголовок с временем и источником, тело этапа
  StatusBar.tsx     липкая полоса: часы, текущий этап, стоп, повтор, «к схеме»
  nodeFacts.ts      верхние 1–2 факта на узел из stageFacts / agentMeters / payload
  exits.ts          refusal.kind → узел выхода (таблица из 1.2)
src/frontend/src/run/
  replay.ts         запись ленты событий прогона и проигрыватель со сжатием пауз
src/frontend/src/styles/
  map.css  map-node.css  map-wires.css  map-drawer.css  map-status.css
```

Удаляется после переезда: `ui/Rail.tsx`, `ui/RailOrder.tsx`, `useActiveStage.ts`,
`run/StageOutline.tsx`, `Pipeline.tsx`, `styles/bands.css`, `styles/rail.css`,
`styles/outline.css` (часть `.live*` переезжает в `map-drawer.css`), `styles/stage-state.css`,
токены `--band-*` из `base.css`, атрибут `data-band` отовсюду. `run/autoscroll.ts`
сокращается до прокрутки к открытой панели; прилипание к низу больше не нужно —
живой этап всегда виден на схеме. `run/railStatus.ts` → `STATE_WORD` в `map/`.

Не меняется: `run/useRun.ts` (модель состояния), `run/sequence.ts`, `run/revealQueue.ts`
(только выдержка 260 → 450), `run/stream.ts`, все `ui/*` визуализации, `useLiveClock.ts`,
`format.ts`. **Python не трогать**: серверу ничего нового не нужно, всё есть в событиях
и `payload` (`decision.status`, `refusal.kind`, `rounds`, `switched`, `budget`).

Стрелки: пути считаются из DOM-прямоугольников относительно контейнера схемы,
пересчёт по ResizeObserver контейнера и панели, не чаще одного кадра. Никакого `transform`
на узлах (п. 7 плана: подложек больше нет, но правило про `overflow`/`transform`
на контейнерах остаётся ради стабильности измерений).

Движение (все значения через токены `base.css`):

| что | как | длительность |
|---|---|---|
| стрелка при переходе | `stroke-dashoffset` → 0, `--ease` | 320 мс, один раз |
| узел pending → running | рамка, цвет, верхняя линия | 130 мс |
| узел running → done | рамка, цвет | 200 мс |
| терминал исхода | заливка | 200 мс |
| панель раскрытия | `grid-template-rows` | 240 мс |
| форма → узел 0 | высота формы, появление сводки | 240 мс |
| reduced-motion | всё мгновенно, мигание номера → статичная метка «идёт» | — |

Запрещено на схеме дополнительно к п. 10: «марширующие муравьи» на пунктирах, частицы или
точки, бегущие по стрелкам, свечение линий, размытие, неон, градиентные линии, повторное
рисование стрелки при каждом кадре, любая анимация, не отвечающая смене состояния.

Доступность: узлы — `<button aria-expanded aria-controls>`; стрелки `aria-hidden`; скрытый
`<ol>` со словесным состоянием каждого этапа для скринридера («этап 3 из 8, готово, 0,6 с»);
клавиши ← → ↑ ↓ ходят по узлам в порядке потока, Enter/Space раскрывает, Escape закрывает
панель или останавливает прогон (панель — приоритетнее); фокус видимый (`:focus-visible`
уже есть). Контраст всех четырёх состояний карточки и подписей рёбер считается цифрами.

---

## 3. Фазы работ

Параллельно не более трёх агентов, зоны файлов не пересекаются. Каждая фаза заканчивается
`npx tsc --noEmit`, `npm run build`, проверкой в браузере на своём порту (8765 не трогать)
и отчётом «сделано → проверено чем». Сначала весь скелет, потом жизнь, потом полировка —
не наоборот.

### Ф1. Скелет схемы (один агент, зона `map/` + `styles/map*.css`)

- `graph.ts`, `layout.ts`, `useNodeRects.ts`, `wires.ts`, `WireLayer.tsx`, `PipelineMap.tsx`,
  `StageNode.tsx`, `InputNode.tsx`, `TerminalNode.tsx`.
- Схема в `App.tsx` под формой условий; состояния узлов и время — из `RunState`
  через `reachedState`/`stageAt`, как сейчас в `Rail.tsx`.
- Удалить подложки и `data-band`; рейку пока оставить (снимается в Ф2).
- Проверка на живом прогоне: узлы загораются в порядке 1→8, стрелки рисуются один раз,
  терминал исхода совпадает с `decision.status`, при `refuse` красный выход ведёт из
  правильного узла по таблице 1.2 (проверить на сценарии `no_feasible`).


**Дефект после Ф1, чинится в Ф2 (замерено в браузере).** Линии выхода в `refuse` получились
длиннее всей схемы: `exit` = 954 px при ширине схемы 939 px, габарит 756×315 px, пять таких
линий идут понизу через весь блок. Основной ход для сравнения — 331 px. Это ровно та длинная
возвратная линия, которую запрещает решение 0.4, и главный источник визуального шума.
Чинить так: выход из узла — короткий пунктирный отросток вниз (60–80 px) с подписью
«отказ», без ведения линии через всю схему к терминалу. Связь с терминалом `refuse`
держится подписью и подсветкой самого терминала, а не линией. Петля 3→3 сейчас висит над
узлом и не читается как петля — прижать к правому краю карточки 03.
### Ф2. Раскрытие и итог (два агента)

- Агент А — `Drawer.tsx`, `map-drawer.css`, переезд `.live*`; `Section` → тело без шапки
  в восьми `stages/*Stage.tsx`; панель «Итог» из `DecisionStage`; удаление `Pipeline.tsx`,
  `StageOutline.tsx`, рейки, `useActiveStage.ts`, `outline.css`, `stage-state.css`, `rail.css`.
- Агент Б — `StatusBar.tsx`, `map-status.css`, сворачивание формы в узел 0, кнопки
  «Стоп», «Ещё раз» (заглушка до Ф3), «Новый прогон»; клавиатура и `aria` по разделу 2.
- Проверка: каждый из восьми узлов раскрывается и показывает то же содержимое, что
  сейчас показывает соответствующая секция (сверить по списку компонентов в 0.7);
  ни одна оговорка из 0.9 не пропала (grep по текстам).

### Ф3. Живая работа на схеме (два агента)

- Агент А — `nodeFacts.ts`: факты на карточках; узел 7 показывает ход оркестратора и
  ожидание модели; петля 3→3 с «раунд N из 3»; пунктир 7→6 при `replans > 0`;
  `exits.ts`; красный выход при отказе.
- Агент Б — `run/replay.ts`: запись ленты `phase/stage/agent/tick/screen` с временем,
  проигрыватель через те же обработчики `useRun` со сжатием пауз > 2 с до 2 с; подпись
  «повтор записи, паузы сжаты»; выдержка очереди 450 мс.
- Проверка на живом прогоне и на повторе: одинаковые конечные состояния узлов, время
  на узлах после повтора равно измеренному, а не сжатому.


Замечание по удалению подложек (проверено grep-ом до начала работ): токены `--band-line`,
`--band-field`, `--band-hover` используются как обычные токены рамки и фона ещё в восьми
файлах помимо `bands.css` — `agents.css`, `opinions.css`, `severity.css`, `final.css` (6),
`config-groups.css` (3), `layout.css` (4), `print.css` (7), `base.css` (9). Удалять объявления
можно только после замены каждого использования на системный токен (`--line-soft` / `--line`,
`--field`, `--row-hover`), иначе рамки молча потеряют цвет по всему интерфейсу.
В `print.css` строки 31–35 переопределяют подложки в белый, а рамки в `#767676` — это механизм
замены цвета на начертание при печати; после удаления подложек проверить, что печать
по-прежнему даёт белый фон и видимые рамки, а не выкидывать блок вслепую.
### Ф4. Отзывчивость, печать, чистка (один агент)

- Раскладки 2 и 1 колонка; печать; reduced-motion; удаление мёртвого кода и стилей;
  файлы ≤ 250 строк; контраст всех состояний цифрами; `autoscroll.ts` до минимума.
- Проверка в браузере на 1920×1080, 1280×720, 900 и 390 px ширины; печать в PDF.


### Ф4-бис. Масштабирование под ширину экрана — ПОСЛЕДНИМ ЭТАПОМ (требование пользователя)

**Дефект, замеченный пользователем на большом экране (2556 px).** Схема прижата влево
и занимает примерно половину ширины, справа остаётся огромная пустота; карточки и шрифт
не растут вместе с окном. Выглядит незаполненно и дёшево.

Делать это **в самом конце**, после того как вся функциональность и раскладки готовы:
правка масштаба до завершения Ф3/Ф4 бессмысленна, потому что состав и размеры узлов
ещё меняются.

**Причина найдена заранее (grep + замер):** `styles/map.css:25` задаёт схеме
`max-width: var(--dense-max)`, а `--dense-max` = **1600 px**. На экране 2556 px схема
упирается в этот потолок — отсюда пустота справа. Колонки при этом уже гибкие
(`repeat(var(--map-columns), minmax(0, 1fr))`), то есть тянуть их не нужно, нужно снять
или поднять потолок и отдельно заняться размером шрифта.

Что сделать:

- Схема тянется на доступную ширину: снять/поднять `max-width` у `.map` (это и есть
  главная причина). Ширина карточки — не фиксированные 250 px,
  а доля колонки через `minmax()` или `clamp()`.
- Размер шрифта на карточках, номера и подписи рёбер — `clamp()` от ширины окна, чтобы
  на 2560 px читалось с расстояния (сцена — проектор в зале защиты), а на 1280 не ломалось.
- Промежутки между строками и колонками тоже масштабируются, иначе при широких карточках
  схема поплывёт.
- Панель раскрытия и «Итог» — та же ширина, что схема, без отдельного узкого контейнера.
- Проверить на 2560×1440, 1920×1080, 1440×900, 1280×720: ни пустоты справа, ни
  горизонтальной прокрутки, стрелки пересчитываются (ResizeObserver уже есть).
- Ограничение сверху всё же нужно: строка текста не должна превышать 75ch
  (`--measure`), иначе панели станут нечитаемыми. Растягивается схема, а не проза.
### Ф5. Аудиты по скиллам (по очереди, каждый чинит найденное)

- `impeccable critique` → `audit` → `polish` на схему и панели; регистр product.
- `ui-ux-pro-max` — чек-лист доставки: 44 px цели, фокус, контраст, состояния, без эмодзи.
- `emil-design-eng` — невидимые детали: тайминги, курсоры, выравнивание стрелок по пикселю.
- `improve-animations` — лишнее движение, рассинхрон стрелки и узла.
- `frontend-design` — не выглядит ли как шаблонный flowchart-генератор; характер.
- Финальный тест ИИ-слопа из `impeccable`: если по схеме можно сказать «это сделала
  нейросеть», фаза не закрыта.

### Ф6. По решению руководителя (после показа)

- Тёмная тема по токенам `data-flow.html` с полным пересчётом контраста.
- Повтор из сохранённого решения `artifacts/decision-*.json` (сервер уже умеет `--decision`).
- Второй референс руководителя (ряд 01–06 с описаниями) как режим «печатная схема»
  для отчёта — если понадобится.

---

## 4. Критерий готовности

- Схема видна до пуска; «▶ Пуск» запускает настоящий прогон; узлы загораются строго
  по порядку событий; стрелки рисуются один раз; исход совпадает с решением.
- Каждый из восьми узлов раскрывается; «Итог» открыт всегда; ни одна визуализация
  второго круга и ни одна оговорка честности не потеряна.
- Ни одной цветной подложки; один акцент; контраст AA цифрами во всех состояниях.
- Три раскладки и печать работают; `prefers-reduced-motion` даёт ту же информацию без движения.
- `tsc` чист, `npm run build` проходит, файлы ≤ 250 строк, ноль комментариев,
  зависимости только react и react-dom, Python не изменён.
- Пять аудитов по скиллам пройдены, найденное исправлено, тест ИИ-слопа пройден.

---

## Ход работ (обновляется по мере закрытия фаз)

### Ф1 — закрыта
Схема построена: узлы змейкой, стрелки, подписи контуров, подложки удалены
(токены заменены на системные в 9 файлах, не удалены вслепую). Проверено лично:
ноль остатков `--band`, ноль запрещённых эффектов, `tsc` чист, Python не тронут.
Узел «Модель прогноза» отменён — сервер не шлёт о ней ничего.

### Ф2 — закрыта
Раскрытие узла в панель, панель «Итог», липкая полоса, сворачивание формы.
Старая лента, рейка, `useActiveStage` удалены. Длинные линии выхода (954 px при
ширине схемы 939) заменены короткими отростками. Убрано лишнее ребро 08 → refuse.

### Ф3 — закрыта
Живые факты на карточках из `stageFacts`; узел 07 показывает ход оркестратора
и кого ждём; повтор записи (`run/replay.ts`); выдержка очереди 260 → 450 мс.

### Дефекты, найденные мной сверх отчётов агентов (починены)

1. **Ветка «ждём агента» была мертва.** Код проверял `kind === "tool"`, а сервер шлёт
   `kind: "consult"` (`orchestrator.py:77`). Соседний `orchSteps.ts` учитывает оба
   значения, новый код — только одно. Замерено: почти две минуты карточка показывала
   «ход 1 из 5» без признаков жизни. После правки видно «ждём агента качества 8,7».
2. **Тип врал и ронял React.** `deployment_readiness` объявлен обязательным, хотя
   сервер формирует его не всегда → `Cannot read properties of undefined` и белый экран.
   Поле помечено необязательным, оба использования закрыты честным текстом.
3. **Узлы 07 и 08 не показывали итог** — всегда намерение. Добавлены `decisionCaption`
   (из `status_label` и `plan_id`) и итог агентов (`outcome`, переопределение выбора).
4. **Скринридер сообщал «0,0»** там, где на карточке `<0,1`. Правило было продублировано
   в четырёх местах; сведено к `spanText()` в `format.ts`. В `StageNode` для работающего
   этапа правило намеренно не применяется — растущее время показывается как есть.
5. **`MarginBars.tsx`**: локальная функция называлась `window` и затеняла глобальный
   объект — из-за этого `npm run dev` давал белую страницу (проблема висела с прошлого круга).

### Ф4 и Ф4-бис — закрыты

Раскладки 4/2/1 колонки (границы 1100 и 720 px), печать переписана
(`print.css` + `map-print.css`; при печати схема принудительно в одну колонку и все
панели раскрыты — CSS не может раскрыть то, чего нет в DOM), `autoscroll.ts` сокращён
со 121 строки до 6: осталась только прокрутка к открытой панели.

**Масштаб под большой экран.** Причина была в `max-width: var(--dense-max)` = 1600 px.
Введён отдельный токен `--map-max: 2320px` — глобально поднимать `--dense-max` было нельзя,
он используется ещё в шести местах. Замерено на 2556 px: схема 939 → **2320 px**,
карточка 250 → **522 px**, заголовок 22 px, номер 32 px, горизонтальной прокрутки нет.
Шрифты переведены на `clamp()` от ширины окна (сцена — проектор, читаемость с расстояния).

**Дефекты, найденные и починенные мной после отчётов агентов:**

- `.map__terminals { grid-column: 3 / span 2 }` верно только для четырёх колонок; на 2 и 1
  терминалы уезжали в неявные колонки и на 390 px наезжали на узел «Решение».
  Добавлено `grid-column: 1 / -1` в медиа-запрос. Проверено: пересечений 0, за экран не выходят.
- Подписи рёбер имели фиксированный `font-size: 11px` — не масштабировались. Переведены на `clamp()`.
- В `autoscroll.ts` остались четыре пустые функции-заглушки, а в `useRun.ts` — мёртвая
  механика `followsUser`/`resumeFollow` и восемь вызовов, которые ничего не делали.
  Агент не мог это убрать (чужая зона), вычищено мной.

### Ф5 — аудит движения и деталей закрыт

**Главная находка: регрессия длинной линии.** Ребро 07 → refuse снова строилось через
весь ряд — замерено 1278 px горизонтального пролёта под узлами «Решение», «hold»,
«recommend_scenario». Причина: узел 07 лежит в одной строке с терминалами, и код брал
ветку `sameRow`, обходя починку Ф2. Ветка убрана, все выходы безусловно короткие.
Проверено лично после правки на 2560 px: `exit` = **44 px** у всех четырёх (было до 1278),
`loop` = 85, `flow` = 691, схема 2320 px.

Оставшаяся длинная линия — `back` «снова Gate», 691 px: это возврат от узла 07 (левый низ)
к узлу 06 (правый верх) между разными строками, короче быть физически не может. Путь
чистый (вверх, вбок, вверх), подпись на месте. Не дефект.

Добавлен `:active` (`scale(0.98)`) на узлы и кликабельные терминалы по конвенции кнопки
«Пуск», с отключением под `prefers-reduced-motion`.

### Ф5 — три аудита закрыты

**Аудит impeccable.** Главная находка — смысловая путаница: технический обрыв потока
назывался тем же словом «отказ», что и легитимный исход `refuse`. Для продукта, чей тезис
«отказ — это результат расчёта, а не сбой», это прямое противоречие, и его читал ещё и
скринридер. Заменено на «обрыв связи»; «отказ» теперь принадлежит только исходу.
Также: контраст рамки узла в покое был 1.50 (порог 3.0 для границ) — схема до пуска
это первое, что видит жюри; переведено на `--muted`, стало 7.30. Возврат фокуса на узел
после закрытия панели (был `<body>`). Подпись контура повторялась до 4 раз на узких
раскладках — теперь один раз на группу.

**Аудит движения.** Регрессия длинной линии (см. выше) и добавлен `:active` на узлы.

**Аудит подачи.** Правок не потребовалось, тест ИИ-слопа пройден.

**Проверено мной после аудитов:**

- Замечание про импорты в `nodeFacts.ts` — ложная тревога: импорты идут подряд строки 1–16,
  grep агента поймал продолжение многострочного блока.
- Замечание про переполнение карточки длинным текстом — справедливо, у `.mapnode__artifact`
  не было ограничения при фиксированной высоте карточки. Добавлен `line-clamp: 3`.
- Слипшаяся подпись «вето сужаетпоиск» — артефакт замера через `textContent`; в SVG это
  два корректных `tspan`. Но межстрочный интервал был жёстким `dy=12` при шрифте `clamp()`
  до 14 px — на большом экране строки наехали бы. Переведено на `1.15em`.
- Пороги честности (25 `Empty`, 46 оговорок) держатся и распределены по 14 файлам,
  а не сосредоточены в одном месте.

---

## План выполнен

**Дефект повтора, найденный и починенный мной последним.** При повторе время на узле
агентов показывало `3:41,6` вместо записанного `3,0`. Причина: `useLiveClock` добавляет
к серверной отметке реальный дрейф (`performance.now() - lastFrameAt`). В живом прогоне
это верно — сглаживает секундные тики; в повторе паузы сжаты, дрейф не соответствует
записи, и время убегало. Часы и счётчик молчания сервера привязаны к флагу `run.live`.
Попутно: «последний ответ сервера N назад» в повторе было ложным утверждением —
сервера там нет. Проверено на `no_feasible`: после повтора время совпало с измеренным,
подпись «повтор записи, паузы сжаты» на месте.

### Критерий готовности — проверено

| Требование | Факт |
|---|---|
| Схема видна до пуска, узлы загораются по событиям | да, проверено на живых прогонах |
| Восемь узлов раскрываются, «Итог» открыт всегда | да |
| Ни одной цветной подложки, один акцент | 0 остатков `--band` |
| Контраст AA цифрами | рамка узла 7.30, текст 6.67–13.6 |
| Раскладки 4/2/1 и печать | 2560 / 1920 / 1440 / 1280 / 900 / 390, PDF A4 |
| `tsc` чист, сборка проходит | да |
| Файлы ≤ 250 строк, ноль комментариев | 0 нарушений |
| Зависимости только react и react-dom | да |
| Python не изменён | `git status src/neftecode/` пуст |
| Оговорки честности | 25 `Empty`, 46 «не передавалось» |
| Тест ИИ-слопа | пройден |

### Осталось по решению пользователя

Ф6 (тёмная тема, повтор из сохранённого решения) — только после показа руководителю.
Светлая тема выбрана основной сознательно: сцена — проектор в зале защиты.
