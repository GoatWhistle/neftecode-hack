# Модель отклика гидроочистки: исследование

Дата: 2026-09-15. Вопрос: можно ли по истории честно построить

```
g(реальное состояние_t, предлагаемое действие) → сера после ГО через лаг ± неопределённость
```

и сравнивать им несколько действий из одного реального состояния.

Всё воспроизводимо: скрипты `context/response-research/s1…s7*.py`, общий загрузчик `common.py`,
прототип решения `response_prototype.py`, сырые результаты `context/response-research/out/*.json`
(каталог `out/` не коммитится, пересоздаётся скриптами за ~5 минут), рабочий журнал `context/response-research/notes.md`.

---

## 1. Что исследовано

1. Код: прогноз `infrastructure/ml/forecast.py`, признаки и сплит `infrastructure/data/data.py`, риск `ml/risk.py`,
   связь с советчиком `infrastructure/live/advisor.py`, модель последствий ГО `domain/production/process.py`,
   планировщик `application/use_cases/plan_operation.py`, gate `domain/advisory/gate.py`, сценарии `config/scenarios/*.json`.
2. Данные: телеметрия 24-2000 и АВТ, ПАК, ЛИМС (все 54 колонки), лист КИП, ответы экспертов (сообщения 517/518/520, Q&A 11.09).
3. Эксперименты:
   - s1 — смысл тегов и режим работы;
   - s2 — какие переменные реально меняются и как;
   - s3 — лаги: взаимная корреляция, event study, ARX, обратная связь;
   - s4 — обучающая выборка;
   - s5 — модели A/B/residual/сценарная/гибрид на временных периодах;
   - s6 — идентифицируемость эффекта (double ML, «неспровоцированные» действия, проверка на ЛИМС);
   - s7 — контрфактические таблицы, монотонность, support gate, покрытие интервалов.

## 2. Какие данные реально доступны

| Источник | Что есть | Замечания |
|---|---|---|
| `task/data/242000_tags.csv` | 26 тегов ГО, шаг 10 мин, 2023-01-01 … 2026-08-07, 189 217 строк | Есть. Описания в КИП не совпадают с величинами (см. §3) |
| `task/data/avt_tags.csv` | 71 тег АВТ | Есть. Для отклика ГО не нужен: между АВТ и ГО резервуарный парк |
| ПАК xlsx | сера ГО, шаг 10 мин, 189 649 точек; D15 с 2025-03 | 7.5% времени ПАК «завис» (плато ≥1 ч) |
| ЛИМС xlsx | сера после ГО 1462; T95 1292; сырьё ГО: T95/T50/КК ≈1276, **сера сырья 132** | Время выдачи неизвестно (принято 4 ч) |
| Схемы | только АВТ | **Схем 24-2000 нет** |

**Отсутствует и не придумывалось:**
- схема 24-2000 с тегами;
- журнал уставок, режимов APC и действий оператора;
- температуры слоёв катализатора / WABT, чистота и парциальное давление H₂;
- частая сера сырья;
- даты замены или регенерации катализатора;
- паспортные ограничения.

Работа установки: в работе 93.8% времени, стабильная работа 84.8%. Длинные остановы:
- 2024-03-16…04-22 (885 ч, после него температура упала на 19 °C — похоже на замену катализатора);
- 2026-04-15…27;
- 2026-06-16…07-02.

## 3. STATE: выбранные теги

**Смысл тегов 24-2000 проверен на данных, а не взят из текста КИП.** Эксперт в сообщении 520 назвал P8 температурой,
T11 — массовым расходом, F19 — давлением. Данные этому противоречат, и чтение «буква тега = тип величины» подтверждается:

| Тег | Чтение по данным | Доказательство |
|---|---|---|
| `T11` (и `T6`) | температура на входе реактора, °C | медиана 363.6, корр. с T5/T6 0.99; ПАК реагирует на неё с верным знаком |
| `T5` | температура выше по ходу (выход/слой), °C | стабильно выше T11 на 5.7 °C (экзотерма) |
| `F26` | объёмный расход сырья, м³/ч | медиана 256 |
| `F19` | массовый расход сырья, т/ч | F19/F26 = 0.832 (IQR 0.824–0.840) — плотность ДТ; ×1000 совпадает с D15 ЛИМС (−3.7 кг/м³) |
| `P13` | давление в реакторе, МПа | 3.92 ± 0.08 |
| `P8` | перепад давления по реактору | 0.17, корр. с расходом 0.68 |
| `F2` | циркуляционный газ | ~93 000; F2/F26 ≈ 370 нм³/м³ — типичное H₂/сырьё |
| `F22`, `F25` | потоки газа/ВСГ | F22 меняется ступенями |

Это рабочая гипотеза с сильной опорой на данные. **До защиты её нужно подтвердить у организаторов** (§22).

Минимальный STATE (известен в момент t, только прошлые окна):
- **режим реактора:**
  - `T_in` (T11, среднее за 1 ч), `T_out`, `dT_reactor`;
  - `T_in_7d` и `T_rel7d` — отклонение от недельного уровня; это прокси активности катализатора;
  - `cycle_age_d`;
- **нагрузка:** `feed` (F26), `density` (F19/F26), `h2oil` (F2/F26), `gas22`, `gas25`, `quench`, `P`, `dP`, изменения за 6 ч;
- **сера:** ПАК за 1 ч, тренд за последний час, среднее за 6 и 24 ч, разброс за 6 ч, последний ЛИМС и его возраст;
- **сырьё:** последние T95/T50/КК/помутнение сырья ГО из ЛИМС (с задержкой 4 ч).

Итого 33 признака, список `STATE` в `common.py`.

## 4. ACTION: выбранные воздействия

| Кандидат | Решение | Причина |
|---|---|---|
| **ΔT на входе реактора (T11)** | **ДА, основное** | Меняется заметно (|ΔT за 1 ч| ≥ 2 °C — 6.6% часов); эффект с верным знаком найден всеми методами, в том числе на ЛИМС |
| **Δрасход сырья, % (F26)** | **ДА, с малой поддержкой** | Движения почти не предсказуемы по состоянию (R² 0.04), знак эффекта устойчив. Но в истории почти нет случаев «расход изменили, температуру удержали»: APC сразу двигает T |
| ΔH₂/сырьё | НЕТ | На 80% это изменения расхода (корр. часовых Δ −0.80), циркуляционный газ почти постоянен |
| ΔF22 | НЕТ | Эффект ~0 и нестабилен |
| Δдавление | НЕТ | Разброс 0.08 МПа, это не управляющее воздействие в данных |

## 5. Как действия определены по временным рядам

- Сетка решений — начало каждого часа t, только стабильная работа (−7…+4 ч).
- `a_T = mean T11 (t, t+1ч] − mean T11 (t−1ч, t]` — что сделали в следующий час.
- `a_feed = то же для F26 / F26_t`.
- Ступенчатое событие для event study: скачок ≥ порога за час, ровно 3 ч до, удержание 3 ч после.

Шум и действия различимы только по масштабу: медиана |ΔT| за 10 мин 0.18 °C, за 1 ч 0.57, за сутки 2.0, за неделю 3.5.
Чистых ступеней T ≥ 2 °C за 3.5 года — 115 (2023: 51, 2024: 33, 2025: 20, **2026: 11**), ≥ 3 °C — 49.
Расход ≥ 10 м³/ч — 183 ступени.
Диагностический вариант «действие = весь последующий путь T» (`aT_path`) содержит более поздние реакции APC,
поэтому используется только для support gate, но не как признак модели.

## 6. Target

- **Основной:** ПАК, среднее за ±30 мин вокруг t+h, h = 1, 2, 3 ч. Модели предсказывают Δ = y − ПАК(1 ч).
- **Независимая проверка:** ЛИМС (сера после ГО).

ЛИМС не подходит как основной target: 1462 пробы, из них с |ΔT| ≥ 1 после решения — 238.
Для модели действий этого мало.
Ограничение: ПАК — слабый заместитель ЛИМС (максимальная корреляция 0.29, MAE 1.65). Поэтому эффект, найденный на ПАК,
отдельно проверен на ЛИМС (§10).

## 7. Лаги

| Метод | Результат |
|---|---|
| ПАК vs ЛИМС | Лучшее совпадение, когда ПАК берётся за **90 мин до** времени пробы ЛИМС (корр. 0.29; при 0 мин — 0.07) |
| Взаимная корр. часовых Δ | k = −1: +0.36 (сера растёт → через час растёт T, это **обратная связь**); k = 0: −0.48; k ≥ +1: ≈ 0. Пика на 2 ч нет |
| Event study, ступени расхода | ПАК меняется **в первый же час** (+0.73 при +12 м³/ч), затем APC поднимает T, и сера оседает на +0.25 |
| ARX (10 мин, 2023–2025) | Ступень +1 °C: −0.20 ppm за 1 ч, −0.29 за 3 ч, плато к ~3 ч; бутстреп по месяцам за 3 ч [−0.32, −0.24] |
| Double ML по горизонтам | Эффект виден уже на 1 ч, на 2–3 ч стабилен |

**Вывод.** Отклик начинается в пределах 1 ч, основная часть проходит за 1–2 ч, плато — около 3 ч.
Точное «мёртвое время» не идентифицируется: APC реагирует на серу в тот же час, и в часовых данных это смешано.
Сценарный ступенчатый лаг 2 ч для полезного действия консервативен. **Для вредного действия он опасен:**
вред начинается раньше. Решение — асимметричный лаг: вред засчитывается сразу, польза — с 2 ч.

## 8. Размер выборки

- Валидных часов: 24 660 (2023: 7346, 2024: 6173, 2025: 7621, 2026: 3520).
- На горизонт h = 1: train 13 499, val 3728, cal 3856, test 3513.
- Периоды по времени target как в проекте: train < 2025-01-01, val 2025H1, cal 2025H2, test 2026, с защитным зазором 6 ч на границах.

Часы автокоррелированы: эффективное число независимых эпизодов намного меньше. Поэтому все доверительные интервалы
считаются бутстрепом по неделям или месяцам.

## 9. Распределение действий

- a_T: квантили 1/5/50/95/99% = −2.87 / −1.57 / −0.04 / +1.72 / +3.30 °C.
  |a_T| ≥ 1 — 22.9% часов, ≥ 2 — 6.5% (2026: 104 часа, это немного эпизодов).
- a_feed: 1/99% = −5.4% / +5.5%; |a_feed| ≥ 5% — 2.6%.
- corr(a_T, a_feed) за 1 ч 0.07, за 3 ч 0.25. P(|Δрасход| ≥ 5% | |ΔT| ≥ 2) = 24% против 11% в среднем.

## 10. Confounding

Найдены три сильных источника смешения.

1. **Старение катализатора.** Среднеквартальная T на входе: 365 → 378 °C (2023Q1 → 2024Q1), после останова 359, затем снова до 372 (2026Q1).
   ПАК при этом держится на 7.5–9.2. Межгодовая связь «T ↔ сера» отражает активность катализатора, а не эффект уставки.
2. **APC держит серу у цели.**
   - Отклонение ПАК от суточного среднего на +1…2 ppm → T за 3 ч растёт на +1.03 °C, на −1…2 ppm → −0.97 °C.
   - Прошлые значения ПАК повышают R² изменения T с 0.34 до 0.46.
   - R²(a_T | состояние) = 0.32.
   - Ступени T — это реакции: перед подъёмом T идёт всплеск ПАК +0.9, потом сера возвращается.
     Эффект уставки неотделим от возврата всплеска к среднему.
   - Эксперт на Q&A прямо предупредил об этом.
3. **Расход и температура движутся вместе.** После изменения расхода APC корректирует T.
   Поэтому «расход −5% при удержанной T» в истории практически не встречается (§16).

Что сделано, чтобы отделить эффект:
- **double ML** с кросс-фитингом по времени;
- выборка **«неспровоцированных»** действий (без всплеска ПАК перед ними, 24% часов);
- **проверка на ЛИМС** — другой прибор и другой target.

| Оценка эффекта +1 °C, ppm | h = 2 | h = 3 |
|---|---|---|
| Double ML, все годы | −0.23 [−0.26, −0.19] | −0.28 [−0.32, −0.24] |
| «Неспровоцированные» | −0.28 [−0.34, −0.22] | −0.30 [−0.37, −0.24] |
| 2023 / 2024 / 2025 / 2026 | −0.17 / −0.19 / −0.28 / −0.48 | −0.14 / −0.29 / −0.47 / −0.50 |
| 2024, «неспровоцированные» | — | +0.01 [−0.11, 0.18] |
| ЛИМС, residualized | −0.35 [−0.51, −0.22] (горизонт 2 ч) | |
| Эксперт (Q&A 11.09) | −0.3 … −1 | |
| Текущий сценарий (k = 0.085 при 8.4 ppm) | −0.71 | |

Рисунок: `context/response-research/out/fig/effect_T_estimates.png`.

Эффект расхода (ppm на +1%): h1 +0.15, h2 +0.09, h3 +0.06 (затухает из-за компенсации APC).
По годам на h3 — от 0.00 (2023) до +0.21 (2026). ЛИМС: +0.29 [0.20, 0.36].

**Итог.** Знак эффекта температуры устойчив во всех методах и почти во всех годах.
Величина плавает в 3–5 раз между годами и зависит от метода. Это эмпирическая оценка отклика в замкнутом контуре,
**не доказанная причинная модель.** Даже double ML снимает только наблюдаемое смешение.

## 11. Какие модели проверены

Все модели предсказывают Δ = y − ПАК(1 ч), кроме персистентности.

| Модель | Суть |
|---|---|
| persistence | ПАК за последний час |
| A_ridge, A_cb | только состояние (ridge / CatBoost) — аналог существующего прогноза |
| B_ridge, B_cb | состояние + действие |
| R_linear_on_A | residual: линейный эффект действия на out-of-fold остатке A |
| M_scenario | A × текущий сценарный множитель ГО (k = 0.085, n = 0.7, ступенчатый лаг 2 ч) |
| H_fitted_mech | A × exp(−k·ΔT)·(1+ΔF)^n, k и n подобраны на остатках train |
| **H_prior_mech (гибрид)** | A × тот же множитель, k = 0.035, n = 1.0 из совокупности оценок §10 |

LightGBM/XGBoost в окружении нет. CatBoost их заменяет, добавлять их ради количества не стали.
kNN использован как механизм поддержки (§16), а не как предиктор.

## 12. Baselines

Персистентность ПАК, модель без действий (A), существующий прогнозный подход, текущая сценарная модель отклика.

## 13. Temporal validation (test 2026, MAE ppm)

| h | Модель | Все | Спокойные | |ΔT| ≥ 2 | |ΔF| ≥ 5% |
|---|---|---|---|---|---|
| 1 | persistence | **0.810** | **0.584** | 1.690 | 1.609 |
| 1 | A_cb | 0.867 | 0.691 | **1.677** | **1.564** |
| 1 | B_cb | 0.964 | 0.708 | 2.360 | 2.010 |
| 2 | persistence | 1.080 | **0.708** | 2.673 | 2.686 |
| 2 | A_cb | 0.964 | 0.782 | 1.634 | 2.348 |
| 2 | B_cb / B_ridge | 0.959 / 0.976 | 0.786 / 0.831 | 1.586 / 1.548 | 2.381 / 2.268 |
| 2 | M_scenario | 0.947 | 0.776 | 1.544 | 2.149 |
| 2 | **Гибрид** | **0.917** | 0.772 | **1.401** | **2.025** |
| 3 | persistence | 1.203 | **0.785** | 2.989 | 2.285 |
| 3 | A_ridge / A_cb | 0.979 / 1.022 | 0.787 / 0.847 | 1.722 / 1.683 | 1.783 / 1.670 |
| 3 | B_cb | 1.005 | 0.824 | 1.587 | 1.750 |
| 3 | M_scenario | 0.982 | 0.839 | 1.557 | 1.449 |
| 3 | **Гибрид** | **0.967** | 0.838 | **1.367** | **1.286** |

Размеры подвыборок на test: спокойные 1124, |ΔT| ≥ 2 — 104, |ΔF| ≥ 5% — 64.
На validation (2025H1) картина та же, но разрывы меньше. Полные таблицы — `out/s5_models.json`.

## 14. State-only (A) vs state+action (B) — ключевой эксперимент

- **B не даёт стабильного улучшения над A.** h1 — хуже, h2 — вровень, h3 — чуть лучше. На строках с действиями на 2–3 ч
  выигрыш 0.05–0.1 ppm, но это меньше разброса между периодами.
- **Калибровка эффекта** — наклон реального остатка A на заявленный моделью эффект, на строках с действиями:
  - B на 1 ч вне обучения: **−0.33…−0.45 (обратный знак)**. Модель выучила совпадение «T растёт, пока сера падает в тот же час», а это обратная связь;
  - на 2–3 ч наклоны 1.0–2.6, нестабильны по периодам;
  - у ridge коэффициент расхода меняет знак с горизонтом: +19 → +4 → −0.5 ppm на 100%.
- **Вывод.** Исторические переменные действий почти не несут дополнительной предсказательной информации,
  а их «эффект» в черном ящике не переносится между периодами. **Чистая supervised response ML отвергнута.**

## 15. Контрфактические проверки (горизонт 3 ч, `out/s7_tables.md`)

Состояние 2026-01-05 08:00 (демо-момент): T 368.2 °C, расход 244 м³/ч, ПАК 5.97.

| Действие | B_cb | B_ridge | Сценарий | Гибрид mid [lo, hi] | Эффект vs HOLD (диапазон) | Поддержка |
|---|---:|---:|---:|---|---|---|
| HOLD | 7.05 | 6.27 | 7.14 | 7.14 [5.87, 8.69] | 0 | SUPPORTED |
| T−2 | 7.37 | 6.60 | 8.47 | 7.66 [6.05, 10.30] | +0.52 (+0.22..+1.32) | UNSUPPORTED |
| T+2 | 6.87 | 5.94 | 6.03 | 6.66 [4.95, 8.43] | −0.48 (−1.12..−0.21) | UNSUPPORTED (0 аналогов) |
| T+4 | 6.70 | 5.61 | 5.08 | 6.21 [4.18, 8.18] | −0.93 (−2.06..−0.42) | UNSUPPORTED |
| расход −5% | 6.92 | 6.30 | 6.89 | 6.79 [5.03, 8.51] | −0.36 (−1.02..−0.15) | UNSUPPORTED |
| T+2 и расход −5% | 6.74 | 5.97 | 5.81 | 6.33 [4.25, 8.26] | −0.82 (−1.98..−0.35) | UNSUPPORTED |

Состояние 2026-07-29 20:00 (ПАК 15.5): T+2 — **SUPPORTED** (46 аналогов, 28 дней), эффект −0.77 (−1.79..−0.34);
T+4 — LOW_SUPPORT; изменения расхода — UNSUPPORTED.

По 400 состояниям 2026 года:
- **B_cb:** в 41% состояний прогноз немонотонен по T; от +3 до +6 °C прогноз не меняется (деревья не экстраполируют);
  эффект T+2 от −0.40 до −0.03;
- **B_ridge:** монотонен по T, но линеен без насыщения, а по расходу даёт физически **обратный знак**
  (расход −5% → сера +0.03);
- **гибрид:** монотонен и гладок по построению; величина — в пределах коробки данных;
- **красный флаг B подтверждён:** немонотонность и насыщение. У гибрида безумных скачков нет.

**Можно ли доверять разнице кандидатов?**
- Направление: T вверх снижает серу, расход вверх повышает — да.
- Порядок T+1 < T+2 < T+4 по пользе — да, по построению.
- Абсолютная величина разницы — только диапазон с разбросом в 2–5 раз.
- «T+2» против «расход −5%» — **неразличимы**: диапазоны почти совпадают.

## 16. OOD / support gate

Прототип `SupportGate` в `context/response-research/response_prototype.py`.

- **История:** все стабильные часы до момента решения.
- **Состояние:** T_in, расход, H₂/сырьё (стандартизованы). Уровень серы не используется: эффект мультипликативный.
- **Аналог:** соседнее состояние (радиус — медианное расстояние до 200-го соседа) и **удержанное 3 ч** изменение
  того же знака и близкой величины: ±max(0.5 °C, 35%); для расхода ±max(1%, 35%).
- **Жёсткие проверки:** уставка после шага внутри q0.5–q99.5 истории (T 344–384 °C, расход 163–304 м³/ч);
  шаг за час не больше q99.5 (4.7 °C, 8.7%), иначе дробить на шаги.
- **Метки:**
  - SUPPORTED — ≥ 30 аналогов и ≥ 5 дней;
  - LOW_SUPPORT — ≥ 10 и ≥ 3 дня;
  - иначе UNSUPPORTED;
  - HOLD всегда SUPPORTED: модель отклика в нём не используется.

Проверка на 2026 году (история 2023–2025):

| Действие | SUPPORTED | LOW | UNSUPPORTED |
|---|---:|---:|---:|
| T+1 | 16% | 20% | 64% |
| T+2 | 5% | 16% | 78% |
| T+3 | 0% | 13% | 87% |
| T+4 | 0% | 3% | 97% |
| расход ±5% | 0–1% | ≤5% | ≥95% |

Метка связана с реальной ошибкой: MAE гибрида на реальных переходах 2026 года — SUPPORTED 0.77, LOW 0.92, UNSUPPORTED 1.04.

**Стратегия:**
- UNSUPPORTED → **блок кандидата**: gate даёт `unknown`, как уже принято в проекте для неизвестного критичного свойства.
  Откат на сценарную модель без поддержки данных запрещён: это была бы та же экстраполяция с другой подписью.
- LOW_SUPPORT → допускается только по худшему краю коробки эффекта.
- SUPPORTED → тоже по худшему краю (gate), середина — только для отображения.

## 17. Uncertainty

Интервал = интервал базового прогноза (split-conformal по |log-остатку| на 2025H2, 90%) × крайние значения множителя
по коробке k ∈ [0.015, 0.085], n ∈ [0.4, 3.0].

| Покрытие на test 2026, h3, цель 90% | Все | Спокойные | С действиями | Ширина (с действиями) |
|---|---|---|---|---|
| A_cb | 0.83 | 0.86 | 0.71 | 3.6 |
| B_cb | 0.82 | 0.85 | 0.69 | 3.4 |
| Сценарий | 0.80 | 0.83 | 0.68 | 3.2 |
| **Гибрид с коробкой** | **0.87** | **0.87** | **0.85** | 4.5 |

Только гибрид близок к номиналу на строках с действиями. Недопокрытие остальных (0.87 и ниже) — сдвиг 2026 года.

Правило для gate:
- кандидат проверяется по **верхней границе** серы;
- польза в сравнении засчитывается по **худшему краю** коробки;
- если граница выше предела или поддержки нет — `unknown`, надёжно безопасным кандидат не считается.

## 18. Ручная модель vs ML vs гибрид

| Критерий | Сценарий (k 0.085, n 0.7, лаг 2 ч) | Чистая ML (B) | **Гибрид** |
|---|---|---|---|
| Знак эффекта T / расхода | верный / верный | T верный, на 1 ч вне обучения обратный; расход меняет знак | верный / верный |
| Величина T | в 2.5 раза выше оценки данных (калибровочный наклон 0.5–0.7) | нестабильна | середина = данные, коробка = разброс лет |
| Монотонность и гладкость | да | нет (41% состояний), насыщение | да |
| MAE на строках с действиями, h2/h3 | 1.54 / 1.56 | 1.59 / 1.59 | **1.40 / 1.37** |
| Покрытие интервала на действиях | 0.68 | 0.69 | **0.85** |
| Экстраполяция | формула без границ | молча плоская | блок через SupportGate |
| Объяснимость | высокая | низкая | высокая: формула + диапазон + аналоги |

Вариант «сценарий + ML-остаток» тоже проверен (R_linear_on_A, H_fitted_mech): остаточная поправка на train занижает эффект
(k 0.010–0.036, n от −0.09 до 2.1 по горизонтам) и нестабильна. **Гибридом выбрана калибровка физической формы по данным,
а не свободная ML-поправка.**

## 19. Вердикт: **C — гибрид**

**Почему не A.** Данные дают устойчивый знак и порядок эффекта T и расхода, в том числе на независимом ЛИМС.
Текущий сценарий переоценивает эффект T в 2.5 раза и занижает ранний вред. Оставить его без калибровки — хуже, чем использовать данные.

**Почему не B.** Модель state + action не улучшает модель state-only стабильно (§14). Эффект «черного ящика» не переносится
между периодами и на 1 ч имеет обратный знак вне обучения; модель немонотонна и насыщается (§15).
Причина — замкнутый контур APC и старение катализатора (§10), а не нехватка алгоритма.

**Почему C.**
- Физическая структура (мультипликативный отклик от ΔT и расхода) с коэффициентами из данных:
  середина — объединённые оценки, коробка — разброс лет и методов.
- Лучший MAE на строках с действиями (h2/h3), единственное близкое к номиналу покрытие (§13, §17).
- Монотонность по построению.
- Отказ вне поддержки.

**Безопасность для оптимизатора.** Можно доверять направлению и грубой величине, а также разнице «ничего не делать — поднять T»
по худшему краю. Нельзя доверять точной разнице между близкими кандидатами разной природы и действиям без поддержки.

## 20. Рекомендуемая архитектура

```
реальная телеметрия/ПАК/ЛИМС ──► проверка доверия (как сейчас)
        │
        ▼
базовый прогноз f(state) — существующий, без действий, со своим conformal-интервалом
        │                                   (HOLD = только он)
        ▼
оптимизатор предлагает шаг: ΔT (T11) и/или Δрасход (F26)
        │
        ├──► SupportGate(state, action) ── UNSUPPORTED ──► кандидат = unknown (блок)
        │
        ▼
множитель отклика ГО r(ΔT, ΔF, h) с коробкой [k, n], асимметричный лаг (вред 0 ч, польза 2 ч)
        │
        ▼
сера притока = верх прогноза × r_худший ─► резервуары / смешение / T95 / цетан / плотность (сценарий, как сейчас)
        │
        ▼
обязательные ограничения (gate): предел по верхней границе, unknown блокирует
```

Граница ML и сценария, оправданная данными:
- **ML:** только базовый прогноз серы после ГО (state-only) и калибровка коробки коэффициентов;
- **данные + физика:** форма отклика и её диапазон;
- **сценарий:** АВТ, запасы, смешение, T95/цетан/плотность, цены, пределы. Для них модели отклика в данных нет.

## 21. Что изменить в существующем коде

1. `config/scenarios/*.json`, блок `stages.hydrotreating`:
   - `conversion_per_degree` 0.085 → 0.035, добавить `conversion_per_degree_range` [0.015, 0.085];
   - `severity_exponent` 0.7 → 1.0, добавить диапазон [0.4, 3.0];
   - `response_lag_hours` разделить на `benefit_lag_hours` 2 и `harm_lag_hours` 0;
   - диапазоны `ht_reactor_inlet_temp_c` [330, 375] → по истории [344, 384]: реальные 2026 года выходят за 375;
   - `current` брать из телеметрии (T11, F26), а не 348/256.
2. `domain/production/process.py::HydrotreatingModel`: хранить коробку; `effective_controls` — асимметричный лаг;
   `run` возвращает оставшуюся долю серы для середины и краёв (перенести `HybridResponse._r/ratio` из прототипа).
3. `application/use_cases/plan_operation.py::_response_ratio` → вернуть худший множитель; в `inflow_properties`
   умножать верх прогноза на него. Та же функция нужна в `lookahead`.
4. Новый `domain/advisory/support.py` (перенести `SupportGate`); в `gate.py` добавить `support_check`:
   UNSUPPORTED → `UNKNOWN` с причиной; LOW → PASS с пометкой.
5. `composition/training.py`: собирать таблицу переходов (часовые state/aT_path3/aF_path3, логика `s4_dataset.py`)
   и сохранять `artifacts/response_support.pkl`. Live-адаптер передаёт историю до момента решения.
6. `config/parameters.json`: `ht_reactor_inlet_temp_c.csv_tag` `ht.P8` → `ht.T11`, `ht_feed_flow_m3h` → `ht.F26`,
   статус «по данным, требует подтверждения», ссылка на этот отчёт.
7. `evaluation/robustness.py`: вариант «отклик слабее на 20%» заменить краями коробки (k_lo, n_lo).
8. Тесты:
   - монотонность множителя;
   - асимметричный лаг (вред виден в t = 0);
   - UNSUPPORTED блокирует план;
   - HOLD не зависит от коробки;
   - запрет применения на истории после момента решения.
9. `explain.py` / экран: показывать «эффект −0.5 ppm (−1.1…−0.2), аналогов 46 за 28 дней» и напоминать, что это изменение
   уставки для контура APC.

Предсказательную часть существующего прогноза менять не нужно.

## 22. Риски на защите

- **Смысл тегов 24-2000** расходится с сообщением 520 эксперта. Нужно подтверждение, иначе вся модель отклика висит на гипотезе.
- **APC.** Реальная рекомендация — это смена цели или уставки APC, а не ручной «T+2». Динамика контура не моделируется.
- **Эффект — оценка в замкнутом контуре**, не причинно доказанный. Величина плавает в 3–5 раз по годам, возможно из-за состояния катализатора.
- **ПАК слабо согласован с ЛИМС** (корр. 0.29). Эффект на ЛИМС совпал по знаку и порядку, но на небольшой выборке.
- **2026 год уже просматривался**: это не слепой тест.
- **Поддержка редкая.** Большая часть шагов ≥ 2 °C и почти все изменения расхода — UNSUPPORTED, поэтому советчик будет часто
  отказываться от действий ГО. Это честно, но уменьшает зрелищность демо.
- **Коробка задана вручную** по совокупности оценок, а не одной формулой.

**Чего не хватает и какой эксперимент сделать после получения данных:**
1. Схема 24-2000 с тегами → подтвердить T11/F26/F19; повторить s1–s7.
2. Журнал APC (вкл/выкл, цели CV, уставки MV против PV) и действий оператора:
   - отделить ручные ступени от реакций контура;
   - оценить эффект на интервалах с выключенным APC;
   - использовать смену цели APC как инструментальную переменную;
   - сузить коробку k.
3. Даты замены/регенерации катализатора и WABT → сделать k функцией активности, а не годом.
4. Частая сера сырья или АВТ-ДТ → проверить мультипликативную форму по сырью.
5. Лучше всего — **плановые ступенчатые испытания**: при выключенном APC ±2 °C с удержанием 6 ч, 6–10 ступеней в разных
   режимах; расход ±5% при удержанной T. Это единственный способ получить причинную оценку.

## 23. Как объяснить жюри

«Мы проверили, можно ли выучить отклик серы на уставки из истории. Нельзя — чёрным ящиком: на установке работает APC.
Температуру поднимают *потому что* сера выросла, и модель, обученная напрямую, путает причину и следствие.
На часовом горизонте вне обучения она даже даёт обратный знак. Поэтому мы не используем ML для “что если”.
Мы взяли физическую форму отклика гидроочистки и откалибровали её коэффициенты по данным методами, которые снимают
обратную связь: double ML, неспровоцированные действия, независимая проверка на лаборатории.
Получили примерно −0.3 ppm на +1 °C (в разные годы от −0.14 до −0.7). Это совпадает с оценкой эксперта 0.3–1 ppm.
Советчик сравнивает действия по худшему краю этого диапазона, а действия, которых не было в истории похожих режимов,
блокирует. ML остаётся там, где он честно проверен, — в прогнозе серы без действий».

---

## TL;DR

- **Задача модели:** из реального состояния сравнить HOLD и шаги ГО по сере после ГО через 1–3 ч с неопределённостью.
- **Получилось ли из данных:** частично. Знак и порядок эффекта — да, устойчиво, в том числе на ЛИМС.
  Надёжная supervised-модель «состояние + действие → сера» — **нет**: APC-обратная связь и старение катализатора.
- **Поддерживаемые действия:** ΔT на входе реактора (тег T11, шаг ≤ 4.7 °C за час) — основное;
  Δрасход сырья (F26) — формально, но почти всегда UNSUPPORTED. H₂/сырьё, газ, давление — не поддерживаются.
- **Target:** ПАК (±30 мин вокруг t+h) для обучения, ЛИМС для независимой проверки. Интеграция — поверх текущего прогноза ЛИМС-серы.
- **Лаг:** отклик начинается в пределах 1 ч, плато около 3 ч; точное мёртвое время не идентифицируется.
  В оптимизаторе вред засчитывать сразу, пользу — через 2 ч.
- **Победитель:** гибрид (вердикт **C**) = базовый прогноз без действий × exp(−k·ΔT)·(1+ΔF)^n,
  k = 0.035 [0.015, 0.085] 1/°C (≈ −0.3 ppm/°C), n = 1.0 [0.4, 3.0].
  Лучший MAE на строках с действиями (h3: 1.37 против 1.59 у ML и 1.56 у сценария), покрытие интервала 0.85 против ~0.7.
- **Доверие:** направлению и порядку T+1 < T+2 < T+4 — да. Абсолютной разнице — только диапазоном (разброс 2–5 раз).
  Разнице между разными типами действий (T против расхода) — нет.
- **Unsupported:** кандидат блокируется (`unknown`), без отката на сценарную формулу. LOW_SUPPORT — только по худшему краю.
- **Остаётся сценарным:** АВТ, резервуары, смешение, T95, цетан, плотность, цены, пределы, динамика APC.
- **Внедрить:**
  1. коробку k/n и асимметричный лаг в `HydrotreatingModel` и сценарии;
  2. худший множитель в `_response_ratio`;
  3. `SupportGate` в gate (unknown при UNSUPPORTED) и таблицу переходов в артефакты обучения;
  4. привязку T11/F26 и реальные диапазоны в `parameters.json`;
  5. подтвердить у организаторов смысл тегов 24-2000 и запросить журнал APC.
