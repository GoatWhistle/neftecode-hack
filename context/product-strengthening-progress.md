# Журнал выполнения: четыре продуктовые доработки (U1–U4)

Начато 22.09.2026, ветка `main`, база `42d34b9`. Push не выполняется.

| Часть | Статус |
|---|---|
| Подготовка | выполнено |
| U3 тяжесть режима | выполнено (backend + тесты; UI-подпись — в U1) |
| U1 парное сравнение | выполнено (UI + логика + тесты; браузерная проверка заблокирована) |
| U2 карта планов | выполнено (backend + SVG/таблица; браузер не проверен) |
| U4 протокол | выполнено (экспорт/импорт/повтор без сети — vitest; браузер не проверен) |
| Общая приёмка | частично: pytest/vitest/build — да; браузер — заблокирован; live — см. ниже |

## Подготовка

- Исходная точка на scripted (порт 18901, `LLM_PROVIDER=scripted`): `context/product-strengthening-baseline/{normal,risk,bad}.json`
  (полный ответ `/api/decide`) и `summary.json`.
  Норма: baseline / 20260105-080000 / healthy → hold, тяжесть 0.0. Риск: sour_crude / 20260724-030000 / healthy →
  recommend_scenario c0042, стоимость 1.158, тяжесть 0.0. Плохие данные: baseline / 20260416-101000 / both_broken → refuse.
- RunRecord (frontend-запись завершённого прогона, спроектирована в U1/U4): версия схемы, run_id, время, условия запроса
  и фактически применённые (`applied`, `injection`, `snapshot`, `binding`), fingerprint входов, версия модели/отпечаток
  данных, версия кода + признак изменённого дерева, provider/model, payload и события. Метаданные собирает backend
  (`run_meta` рядом со screen payload); бизнес-логика во frontend не дублируется.

## U3 (коммит 98e3435)
Профиль `severity-profile/1`, отчёт до/после — `context/product-strengthening-u3-report.md`. Тесты:
`tests/infrastructure/test_severity_profile.py` (7), заморозка решений сохранена (хеш считается без добавленных
ключей `severity`, `tradeoff`; эталоны не переписаны). Решения трёх сцен не изменились, тяжесть 0 → 0.514/0.535.

## U2 backend (тот же коммит 98e3435)
`domain/advisory/tradeoff.py`: недоминируемое множество по пулу после Gate/ограничений/предела тяжести, точность 1e-6,
равные планы сведены в одну точку, неизвестные метрики исключены с причиной, стресс-проверки только у выбранного.
В решении блоки `severity` и `tradeoff` (после decision_id). Тесты: `tests/domain/test_tradeoff.py` (9),
`tests/application/test_decision_tradeoff.py` (3). На поставляемых сценариях фронт вырождается в одну точку
(выпуск 300 у всех, есть недоминируемый вариант с нулевой тяжестью) — UI так и пишет; выбранный ядром план
(hold/c0036) доминируется, причина показана.

## RunRecord metadata (ca4e451 + правка слоёв)
`presentation/web/run_meta.py`: `run_meta` в payload (условия запрошенные/применённые, отпечаток входов, версии
кода/модели, provider, горизонт). Git/файлы читает `infrastructure/artifacts/provenance.py`, внедряется композицией
(тест слоёв требует). Тесты `tests/architecture/test_run_meta.py`.

## U1/U4 frontend
Файлы: `run/{changes,record,pair,protocol,sha256,hydrate,presets}.ts`, `compare/{PairCompare,TradeoffMap,tradeoffView}`,
`whatif/{WhatIf,ProtocolBar,RecordBanner}`, `scenes/SceneBar`, правки `App.tsx`, `useRun.ts`, `OperatorAnswer`, `Verdict`.
Vitest: 56 тестов (было 29): пары (отказ, горизонт/срез, неизвестное, присадка, вариативность LLM, заменённый ввод,
неизменяемость A), протокол (круг, отказ, A/B, лишние поля, повреждение, версия, контрольная сумма, нечисла),
открытие записи без fetch/SSE, карта, ответ оператору. Воспроизводимые пары: risk → reserve выведен = отказ;
risk → frozen_pak = hold; risk → stale_lab = без изменения (`src/frontend/src/fixtures/pair/`).
Исправления подачи: подписи сцен сбрасываются, недоступный срез не подменяется, отказ без «Уставки не менять»,
caveat времени рядом с ожиданием, рецепт в % масс., обобщение «не измерения завода» заменено.

## Не выполнено / ограничения
- **Браузерная приёмка (1440/390 px, production dist, перезагрузка) не выполнена: расширение Chrome не подключено**
  («Browser extension is not connected», две попытки). Визуальное качество, SVG, мобильная вёрстка не проверены.
- Не сделано: замена «остановка отображения» подписей везде, полная перекомпоновка «единый ответ» вне указанного,
  отдельный экспорт `usage/cost` (у провайдера не возвращаются).
- HTTP-стек (4 процесса): `run_meta` и блоки формируются только в `neftecode serve`.

## Проверки (итог)
- `LLM_PROVIDER=scripted uv run pytest -q tests` — **1431 passed, 314 с**; frontend `npm test` — 13 файлов, 56 тестов; `npm run build` — успешно (dist пересобран).
- Live (`neftecode serve`, zai / glm-5.3-flash, `/api/decide`, ключи не выводились):
  - плохие данные: refuse, agents skipped/data_refusal, **0 LLM-вызовов**, 0.04 с.
  - риск sour_crude 24.07, попытка 1: LLM timeout на первом вызове → fallback, план c0042, токены 0 (122 с).
  - попытка 2 (602 с): 11 LLM-вызовов, 45 636 токенов (33 493+12 143), стоимость провайдером не выдана;
    агенты приняли 4 ограничения (запас качества 1.0, ≥12 ч до нарушения, ≤1 изменения, не хрупкий) и veto c0006 (качество);
    итог **refuse** (`orchestrator_no_final:budget:timeout`, допустимых планов при ограничениях нет). Значит на live агенты
    изменили решение относительно scripted (c0042) — но это один прогон, недетерминированный; польза LLM не доказана.
  - live не сохранён в repo (JSON в scratchpad). Экспорт протокола из live в UI не проверялся (нет браузера).
