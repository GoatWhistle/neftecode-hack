# Журнал агента 2 (фронт)

Ветка `task/audit-frontend`, worktree `/Users/kaifarikman/hackathons/neftecode-frontend`.

## Стартовая интеграция на новую базу (21.09.2026, продолжение после паузы)

Прежняя пауза («жди правки фронтендера») снята явным уточнением в `parallel-frontend.md`:
новая стартовая база — `733661f` (backend-пакет) + `16e02f0` (новый frontend с графом
агентов). Выполнено по инструкции уточнения:

```sh
git merge --ff-only 733661f
git merge --no-edit 16e02f0
```

Оба без конфликтов (`ort` strategy на втором, только `PIPELINE_PLAN.md` auto-merged).
Черновой некоммиченный F1 из прежней паузы сохранён и доведён до готовности, не переписан
старым фронтом. `task/`, `artifacts/` скопированы (не symlink) локально в worktree для
фикстур — в `.gitignore`, не коммитятся.

## F1 — тестовый runner (0895e99)

`npm test` (vitest run) зелёный. `vite.config.ts` — порт 15173, прокси `/api` →
`127.0.0.1:19865`. `vitest.config.ts`/`setupTests.ts` — jsdom, `cleanup()` в `afterEach`
(без него — «нашлось два элемента» на втором тесте в файле). `format.test.ts`/
`ui/Origin.test.tsx` — под реальное поведение кода, не придуманные ожидания.
Фикстуры сняты с backend этой базы (`LLM_PROVIDER=scripted neftecode serve`, порт 19865
обычный / 19866 `AGENTIC_DECISION_ENABLED=0`), происхождение — `src/fixtures/README.md`.

## F2 — единицы присадки (d685e6a)

`additive_dose` — доля к массе компонентов, не кг/т. `Verdict.tsx`, `StateStage.tsx`,
`PlanDiff.tsx` печатали долю напрямую с подписью «кг/т». `format.ts`: `additiveDoseKgPerT`
(× 1000), `doseDigits` (точность растёт для малых доз, иначе 0.03 кг/т неотличимо от 0),
`doseText`. Регрессии тестами: 0→0, 0.03→30, отсутствует→«—», малая ненулевая доза не
схлопывается, разница в альтернативе — тоже в кг/т.

## F3 — plan_origin/chain (2acbee8)

O1: `Verdict.tsx`/`ChoiceStage.tsx` подписывали значения плана жёстко `origin="derived"`.
Заменено на чтение `explanation.plan_origin.immediate_action`/`.steps[i]`; нет поля — «не
определено», не выдуманный derived. N2: `explanation.chain` — управляемость и основание
модели блока; `StateStage.tsx` помечает неуправляемые уставки, заголовок мок-экрана
показывает реальную цепочку вместо статичной строки. Уточнена формулировка `derived` в
`provenance.ts`/`Origin.tsx` (не «из данных завода» — сценарный расчёт тоже derived).
Тест на реальной фикстуре 05.01: АВТ=scenario, T6=measured.

## F4 — честные статусы агентов (1c31b7f)

O2 (`agentic_state`) подключено в `types/index.ts`, `run/sequence.ts` (`stageRan`),
`stages.ts` (`agentsLamp`), `run/mode.ts` (`modeOf`), `map/nodeFacts.ts`
(`agentsCaption`), `ui/AgenticMode.tsx`. N1 (`confidence_kind`/`confidence_calibrated`):
убраны ConfidenceBar (5 клеток) и 10-клеточная шкала в `Opinions.tsx`/`ConsultCard.tsx` —
обе выглядели как вероятность; везде — словесный уровень + «самооценка модели, не
калибрована». То же добавлено в `GraphFacts.tsx` (раньше было только в Opinions/ConsultCard).
Новый граф (`graph/model.ts`, `GraphPanel.tsx`, `GraphFacts.tsx`): `agentic.final.candidate_id`
(предложение LLM) больше не выдаётся за итог — используется `decision.selected_plan.plan_id`
отдельным параметром `buildGraph`; текст про отсутствие консультаций больше не утверждает
«нет допустимого плана»; «все согласились» проверяет реальное согласие (все отвечали ACCEPT),
а не только отсутствие fail/warn — UNKNOWN, неотвеченный запрос, невалидное мнение не
считаются согласием.

## F5 — честные подписи Stop/Escape (16a249f)

O4/Z9-1: Stop только обрывает отображение на фронте, расчёт на сервере — daemon-поток без
отмены. «Остановить прогон»/«Стоп»/«Расчёт остановлен» заменены на «Остановить отображение»
везде (`StatusBar.tsx`, `AgentPresentationMock.tsx`), подсказка — «расчёт на сервере может
продолжиться». Убран хардкод «технологический запас — 1 мг/кг» в `ForecastStage.tsx» (числа
в payload нет), заодно убран тот же выдуманный порог из логики tone. HTTP-ошибки и
отсутствие `/api/stream` уже передавались честно (реальный статус) — отдельных правок не
потребовалось.

## F6 — контрактные проверки (2de613d)

SSE (`run/stream.test.ts`) — реальная запись `/api/stream` своего serve
(`stream-baseline-2026-01-05.sse`), не переписанный вручную список: phase→stage→agent→
screen→end, все обработчики вызваны. Плюс негативные потоки: HTTP 404 до старта этапов,
обрыв до `screen`. Маршруты (`fixtures/README.md`): фронт вызывает ровно `/api/stream` и
`/api/options` (проверено grep, не по памяти), оба реально запрошены у своего `serve` живым
curl — не объявляет исправленным известный 404 у `neftecode-stack` (дефект #10, сервер, A6).
Старый payload (`ui/OldContract.test.tsx`) — тестовая мутация без `plan_origin`/`chain`/
`confidence_kind`, `ActionBlock` не падает и показывает «не определено».

## F7 — финал

`npm test`: **29 тестов, 9 файлов, все зелёные.** `npm run build`: `tsc --noEmit` чист,
`vite build` — 139 modules, без ошибок (предупреждение о размере чанка >500kB — не новое,
не в скоупе F1–F7).

Браузерная проверка (Chrome, сервер этой базы на 19865/19866, dev на 15173):
- 1440px, сценарий `риск качества` (sour_crude), агенты реально консультировались:
  шапка мок-экрана показывает реальную цепочку «АВТ (ходы отключены) (сценарий) →
  Гидроочистка (β по данным) → Смешение (материальный баланс)» — F3 chain подтверждён
  живьём. Доза присадки — «0 кг/т» (F2). Полный разбор: АВТ-уставки помечены «сценарий»,
  T6 — «измерение», расход ГО — «расчёт» (F3 O1 подтверждён живьём).
- Основной экран (без query), сценарий `запас по резерву`, `AGENTIC_DECISION_ENABLED`
  включён, live-модель scripted: узел 07 «Агенты» → `AgenticMode` корректно показывает
  «Живая модель не участвовала: детерминированная политика», карта обмена (GraphPanel) —
  оба специалиста ACCEPT, итог узла — «план hold» (не выдуманный candidate_id — F4
  подтверждён живьём), «Обмен целиком: все специалисты приняли план без возражений»
  (честная логика согласия). Узел специалиста: «самооценка модели: высокая (0.80, не
  калибрована по исходам)» (N1 подтверждён живьём). Ошибок консоли не найдено.
- 390px: главный экран и панель условий — двухколоночная адаптивная сетка, без
  горизонтальной прокрутки.
- Тестовые mutation-скрипты (agents-off, old-contract) не запускались живьём в браузере —
  только через vitest-фикстуры; это тестовые мутации, а не реальные прогоны, поэтому не
  требуют браузерного повтора отдельно от unit-тестов.

Backend-инстансы (19865, 19866) и dev-сервер (15173) остановлены после проверки.

## Итог ветки

Коммиты: `0895e99` F1, `d685e6a` F2, `2acbee8` F3, `1c31b7f` F4, `16a249f` F5, `2de613d` F6.
Ветка не смёржена в `main`, не запушена — по инструкции задания. Изменения — только
`src/frontend/**` и этот журнал; Python (`src/neftecode`, `tests/` в корне), `config/`,
общие `context/state.md`/`task-progress.md`/`README.md` не трогались. Полный `pytest` не
запускался — по инструкции, Python этого пакета неизменён.

Осталось (вне скоупа F1–F7, по заданию): T8 (экран резервуаров), G4/G6/G7, серверный SSE
gateway для стека и отмена (A6/A7) — задачи первого агента/следующей волны.
