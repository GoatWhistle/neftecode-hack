# План завершения проекта

Обновлено: 2026-09-20. План составлен после повторного чтения ТЗ, аудита реализации, сравнения
с публичными репозиториями конкурентов и проверки нового интерфейса в браузере, включая один успешный
прогон с живой `Z.AI / glm-5.3-flash`.

## Решение по объёму работ

Функциональное ядро замораживается. Новые агенты, модели, чат, Парето-фронт и большой мониторинговый
dashboard до сдачи не добавляются. Цель оставшейся работы — устранить противоречия интерфейса, сделать
главный ответ понятным за несколько секунд, доказать воспроизводимость и подготовить короткую защиту.

## Очерёдность

### 1. Исправить противоречивые статусы интерфейса — обязательно

- После раннего отказа этап `Решение` должен иметь конечный статус, а не `ожидает`.
- Неисполнявшиеся этапы должны называться `пропущено`, а не `готово`.
- Разделить три режима: живая модель, штатная детерминированная `scripted`-политика и аварийный
  `fallback` после сбоя провайдера.
- Убрать противоречие «сработал запасной путь» / «отката не было» в штатном `scripted`-прогоне.

**Готово, когда:** нормальный прогон, отказ из-за плохих данных, `scripted` и живая LLM показывают
непротиворечивые статусы; это проверено в браузере.

**Статус (2026-09-20, ветка `worktree-finalization-plan` от `a728a10`) — ЗАКРЫТО, проверено пиксельно
в реальном Chrome.** `StageState` получил `skipped` — этапы, до которых прогон не дошёл, теперь
«пропущено», а не «готово» (`run/sequence.ts: stageRan`), включая этап «Агенты»: подтверждено живым
прогоном с `fault=both_broken`, где `agentic.outcome=="skipped"` и trace содержит только `data` — это
нашли и починили именно на живых данных, не в коде «на глаз». Найдена и исправлена реальная причина
противоречия «сработал запасной путь» / «отката не было»: backend (`decision.py`) никогда не шлёт
`agentic.outcome == "ok"` и никогда `mode == "scripted"` — фронт сравнивал именно с этими значениями.

Сначала прогнал логику через SSR (`renderToStaticMarkup` без браузера) — `claude-in-chrome` в сессии
тогда не был подключён (`list_connected_browsers` пуст). Затем расширение подключилось (пользователь
открыл Chrome), и все три сценария пройдены **вживую, с реальным кликом и скриншотом**, живая модель
`zai/glm-5.3-flash` (ключ из `.env`, переменная `token`, с явного разрешения пользователя),
через `uv run neftecode serve`:
1. **`ample_reserve`, все источники исправны** — «Решение принято с участием живой модели», все 8
   этапов «готово», «Причина отката: отката не было» — никакого «сработал запасной путь».
2. **`sour_crude`** (сложный сценарий, см. пункт 5) — то же самое: живая модель, все этапы «готово»,
   цепочка агентов отрендерилась с реальным текстом от Z.AI.
3. **`fault`: лаборатория и анализатор недоступны (`both_broken`)** — «08 Решение: ГОТОВО» (не
   «ожидает» — прямое требование пункта), «03 Кандидаты» и **«07 Агенты: ПРОПУЩЕНО»** (курсивом,
   приглушённым цветом — не «готово»); поле у «Агенты» подписано «Почему агенты не привлекались:
   `data_refusal`», а не «Причина отката» — ровно то текстовое исправление, которое до этого было
   предсказано SSR-рендером, теперь видно на реальном экране.

Побочная находка при первой попытке: `neftecode-stack` (4-процессный gateway) не реализует
`/api/stream`, только `/api/decide` — фронтенд получал 404 и красиво показывал «сервер ответил 404»
(это тоже honest failure, не крах). Правильный процесс для браузерной демонстрации — **`uv run
neftecode serve`** (однопроцессный сервер с `presentation/web/server.py`, где `/api/stream` есть).
Стоит явно отметить в README, какой из двух серверов для чего: `neftecode-stack` — для HTTP-контрактов
между процессами, `neftecode serve` — для интерфейса браузера.

### 2. Поднять ответ оператору над подробной схемой — обязательно

В начале результата компактно показать:

1. вердикт: сохранить режим / изменить режим / отказ;
2. что именно сделать относительно текущего режима;
3. главную причину;
4. обязательное условие или предупреждение перед действием.

Для хрупкой рекомендации предупреждение о потере допустимости и необходимости прямой пробы резервуара
должно быть визуально заметнее идентификатора плана. Схема, трасса и JSON остаются ниже как доказательства.

**Готово, когда:** по первому экрану результата за 10 секунд понятны действие, причина и главное
ограничение без раскрытия технических деталей.

**Статус — ЗАКРЫТО, проверено пиксельно.** `ui/OperatorAnswer.tsx` рендерится в `App.tsx` до
`<PipelineMap>`, все четыре пункта на месте, предупреждение о потере допустимости стилизовано
заметнее остального текста карточки. Живьём в Chrome (`uv run neftecode serve`, три сценария) экран
читается ровно так, как задумано: **«Сохранить режим» крупным заголовком → «Сохранить текущий режим:
изменения уставок не требуются.» → причина одной строкой → рамка с предупреждением о промышленном
применении** — всё это видно на первом экране без скролла и без единого технического термина; то же
самое для отказа — **«Надёжной рекомендации нет» (подчёркнуто, красным) → «Действие не выдано…» →
причина → предупреждение**. Случая с реальным `fragile_plan`/`tank_estimate_sensitive` в доступных
сценариях не встретилось — код-ревью формулировки из `risk_block.py` совпадает буквально, критическая
стилизация (`.answer__note--critical`) кодом проверена, но живого триггера не было.

Независимая проверка нашла два реальных пробела, оба закрыты: 1) при отказе карточка всегда писала
«допустимый план не найден», даже когда поиск вообще не запускался (потеря телеметрии — отказ на
проверке данных, до Gate/optimizer); теперь текст различает все четыре `explanation.kind`
(`bad_data`/`model_not_applicable`/`no_feasible_plan`/`agent_rejected`) точным текстом под каждый.
2) для рекомендации карточка не говорила, что именно меняется («действие — на этапе «Итог» ниже»);
теперь `changeSummary()` сравнивает `immediate_action` с `current_operation` (то же сравнение, что
`PlanDiff` делает для альтернатив, только против текущего режима) и показывает до трёх изменившихся
уставок прямо в карточке, с явным числом остальных, если их больше.

Второй заход независимой проверки на реальном сценарии риска нашёл, что пункт 2 всё же не сработал:
`changeSummary()` брал только `decision.current_operation`, который для этого ответа пуст, хотя
`explanation.current_operation` заполнен (тот же приоритет, что уже использует `StateStage.tsx`).
Исправлено — теперь сначала `explanation.current_operation`, при его отсутствии
`decision.current_operation`; проверено на сохранённом `no_feasible.json`: карточка стала показывать
«Изменить: доля Основной гидроочищенный дизель: 0,9 → 1» вместо отсылки к «Итогу» ниже.

### 3. Убрать внутренние коды из основного пользовательского слоя — обязательно

- Заменить на человеческие подписи `ample_reserve`, `confirmed_legacy`, `synthetic_scenario`,
  `recommend_scenario`, `sulfur_mgkg` и похожие значения.
- Исходные коды сохранить в раскрываемой трассе и JSON для аудита.
- Для агентного результата показывать короткую цепочку: «ядро предложило → специалисты проверили →
  ограничения/вето → итог».

**Готово, когда:** основной экран читается без знания внутренней модели данных, но техническая
проверяемость не потеряна.

**Статус — ЗАКРЫТО, проверено пиксельно.** Переведены `legacy_status`, `outcome` (агентный слой) и
`scenario_id` («Состояние») — коды остались рядом в `<code>`/JSON. Цепочка «Ядро предложило:
сохранить режим → Специалисты проверили: агент надёжности — принял; агент качества — принял →
Ограничения и вето: не добавлялись → Итог: …» реально показана в Chrome на живом прогоне `sour_crude`
с настоящим текстом от Z.AI (не заглушкой). При отказе на данных живьём подтверждено дополнительное
исправление: поле у «Агенты» подписано **«Почему агенты не привлекались: `data_refusal`»**, а не
«Причина отката» — то, что раньше выглядело бы как остаток кода не по месту. Остальной экран (за
пределами примеров из плана) построчно не проверялся — если найдутся ещё внутренние коды на главном
слое, лечится тем же приёмом (словарь + `<code>` рядом).

Независимая проверка нашла ещё три конкретных места: 1) `decision.scope` (`synthetic_scenario`/
`confirmed_model`) показывался сырым текстом в «Итоге» — теперь переведён (`SCOPE_TEXT` в
`Verdict.tsx`); 2) три терминальных узла схемы (`hold`/`recommend_scenario`/`refuse`) показывали код
крупным моно-заголовком, а человеческий текст — второстепенной подписью снизу, наоборот принятой
конвенции — заголовки стали человеческими («Сохранить режим»/«Изменить режим»/«Отказ»), код ушёл в
мелкую пометку рядом; 3) предложенные и наложенные ограничения агентов (`proposed_constraints.type`,
например `min_quality_margin`) рендерились сырым JSON-полем без перевода — добавлен словарь
`constraintTypeText` на весь список типов из `CONSTRAINT_VOCABULARY` (specialist.py), код остался
рядом мелко для аудита.

Второй заход независимой проверки нашёл, что этого было мало: 1) `hold`/`recommend_scenario`/`refuse`
всё ещё были видны текстом на терминальной карточке (просто мельче) — по правилу «код только в
раскрытой трассе/JSON» код убран из видимого текста вовсе, остался только в `title` (нативная
подсказка при наведении); 2) `decision.reason` — детерминированный текст backend (`lookahead.py`),
иногда содержит сырое имя свойства качества прямо в предложении (например «при плане hold
sulfur_mgkg = 10.02 при пределе 10.5»). Backend не трогал (влияет на `decision_id` и замороженные
хеши тестов) — добавлена `humanizeReason()` (`run/orchRead.ts`): находит известные ключи `LIMIT_TEXT`
как целые слова внутри уже готовой фразы и меняет на перевод («сера», «T95» и т.д.), остальной текст
не трогает. Применена в `OperatorAnswer.tsx`; проверено синтетическим тестом на строке из
`lookahead.py` — `sulfur_mgkg`→«сера», `t95_c`→«T95».

### 4. Проверить поставку одной командой в чистом окружении — обязательно

- Собрать именно сдаваемый архив.
- Проверить, что в него входят необходимые модельные артефакты, замороженные срезы и
  `source_rules.json`; отсутствие обязательного файла должно завершать проверку ошибкой.
- Пройти запуск строго по README без ручного исправления путей и файлов.
- Отдельно описать два режима: воспроизводимое офлайн-демо и опциональный запуск с живой LLM.

**Готово, когда:** человек вне команды распаковывает архив, выполняет одну документированную команду и
получает интерфейс с готовым сценарием без доступа к секретам и внешней сети.

**Статус:** сделано. С нуля (свежий git worktree, без кэшей): `uv sync --frozen` →
`uv run pytest -q` — 1357 passed, 2 skipped (1359, как и раньше); `uv run neftecode scenes` без
срезов — 5 сцен и оба предупреждения, как задокументировано в README. Обучение `train`/
`snapshot --all` в этом worktree не запускалось (нет `task/` — 343 МБ выданных данных не трекаются
в git и распространение не согласовано), но обучать заново было не нужно: `artifacts/` (4,2 МБ,
включая `model.pkl`) скопирован только для чтения из основного чекаута, где уже обучен. С этим
`bash scripts/package.sh` прогнан целиком: все обязательные файлы на месте, `expert_grid.json`
(опциональный) корректно пропущен с предупреждением, а не ошибкой; собран `neftecode-20260920-154407.
tar.gz`, 2,2 МБ, 626 файлов — без `task/`, с актуальной сборкой `src/frontend/dist/index.html`.
Проверка «нет секретов / нет обращений к внешней сети / нет тяжёлых исходных данных» встроена в сам
скрипт и прошла. Копия `artifacts/` и собранный тестовый архив остались только в рабочем дереве
(в `.gitignore`), в коммит не попадают.

Независимая проверка нашла, что «отсутствие обязательного файла должно завершать проверку ошибкой»
было верно не для всего списка: `snapshots` и `source_rules.json` были в `OPTIONAL_ARTIFACTS`, поэтому
успешная сборка ничего не говорила об их наличии — сам путь отказа не был проверен. Исправлено:
`source_rules.json` перенесён в `REQUIRED_ARTIFACTS`; для `snapshots` добавлена отдельная проверка
(каталог должен существовать и содержать хотя бы один файл, не просто существовать). Путь отказа
протестирован вживую: `mv` обоих в сторону → `bash scripts/package.sh` → `ОШИБКА: нет обязательного
artifacts/source_rules.json` + `ОШИБКА: artifacts/snapshots пуст или отсутствует` → exit 1; файлы
возвращены на место → сборка снова успешна (628 файлов, оба присутствуют в архиве — проверено `tar tzf`).

### 5. Зафиксировать доказательство мультиагентности — после пунктов 1–4

- Сохранить уже подтверждённый живой прогон: `Z.AI / glm-5.3-flash`, 8 вызовов, 35 075 токенов,
  закрытый поток, без fallback; результат — подтверждение `hold`.
- Один раз проверить живой сложный сценарий, где у специалиста есть основание ужесточить выбор или
  наложить вето.
- Если итог изменился, показать конкретное влияние агента. Если не изменился, честно зафиксировать, что
  специалист подтвердил детерминированное решение; не подгонять демонстрацию.
- Для защиты использовать сохранённую трассу или запись и явно подписать её как запись: живой вызов
  занимает минуты и зависит от внешнего провайдера.

**Готово, когда:** видно не количество сообщений, а роль каждого специалиста и влияние либо отсутствие
влияния на итог.

**Статус — ЗАКРЫТО.** Первый пункт подтверждён: `artifacts/agent-live-specialists-20260917.json`
реально содержит закрытый живой прогон (7 `llm_call`, 34 831 токен, `outcome="confirmed_legacy"`,
без отката) — уже включён в `package.sh`-архив (пункт 4).

Второй пункт выполнен в эту же сессию: пользователь подключил Chrome и разрешил использовать реальный
ключ провайдера (переменная `token` в `/Users/kaifarikman/hackathons/neftecode-hack/.env`, отдельно
от `ZAI_API_KEY`, который в окружении действительно отсутствовал). Через `uv run neftecode serve`
прогнан **живой сложный сценарий `sour_crude`** — специалисты качества и надёжности реально
проконсультированы (в интерфейсе видно «ждём агента надёжности…» во время прогона), итог —
`confirmed_legacy`, 8 `llm_call`, 35 009 токенов (практически совпадает со старой записью
«8 вызовов, 35 075 токенов» — видимо тот же класс прогона). **Вето не наложено**: специалисты приняли
план без ограничений (`vetoed_candidates: {}`, `constraints_applied: []`) — по инструкции пункта это
честно зафиксировано как есть, демонстрация не подгонялась под желаемый исход. Трасса сохранена как
`artifacts/agent-live-sour_crude-20260920.json` и добавлена в `OPTIONAL_ARTIFACTS` в `scripts/package.sh`
— войдёт в сдаваемый архив автоматически.

### 6. Упаковать доказательство качества прогноза — после пунктов 1–4

Сделать один слайд или компактный блок: простой ориентир → наш прогноз → временные фолды → фактическое
покрытие интервала → известные ограничения → пример, где прогноз меняет решение. Не сравнивать MAE с
чужими числами как единый рейтинг: выборки и постановки у команд различаются.

**Готово, когда:** проверяющий понимает методику и границы результата без чтения исследовательских файлов.

**Статус — ЗАКРЫТО, с исправлением по независимой проверке.** `context/forecast-evidence.md`, шесть
блоков, числа из уже проверенных README.md/RESPONSE_MODEL_T6.md. Независимая проверка нашла две
неточности первой версии, обе исправлены: 1) `last_pak`/`last_pak_bc` называли «оценкой серы
резервуара», хотя это прогноз **лабораторной серы потока после ГО** (протокол T103, §1) — отдельный
и не провалидированный тем же rolling-протоколом механизм оценки содержимого резервуара теперь явно
разведён в отдельный раздел 5, а пример в разделе 6 прямо подписан как «пример другого механизма»;
2) «стабильно измеряемый сдвиг прибора» переписано — смещение ПАК–ЛИМС само по себе непостоянно
(медиана по полугодиям от −0.97 до +1.04 мг/кг, `state.md`), поправка пересчитывается по последним
20 пробам каждый раз, а не берётся раз и навсегда.

### 7. Собрать и отрепетировать демонстрацию — финальный этап

Порядок показа не длиннее трёх минут:

1. нормальный режим — осознанное `hold`;
2. риск качества — прогноз, альтернативы, Gate и предупреждение;
3. плохие данные — объяснимый отказ и условие повторного расчёта;
4. краткое раскрытие агентной трассы;
5. честные границы промышленного применения.

После репетиции выполнить production-сборку фронтенда, целевые тесты исправленных состояний и полный
набор тестов, если позволяет время. Зафиксировать команды и результаты проверки.

**Готово, когда:** демонстрация укладывается в три минуты, не требует импровизированного поиска по
странице и воспроизводится из сдаваемого архива.

**Статус — не начат, но есть заготовка по реальным замерам этой сессии.** В браузере вживую пройдены
сцены 1 (нормальный `hold`, `ample_reserve`) и 3 (плохие данные, отказ на `both_broken`) — обе за
секунды; сцена сложного агентного сценария (`sour_crude`, ближе к «риску качества» пункта 2, хотя не
из snapshot 24.07.2026) заняла **≈3 мин 03 с** живого вызова Z.AI целиком, из них этап «Агенты» —
основная часть времени (несколько ходов оркестратора + консультация обоих специалистов). Это значит:
**полные три сцены с живой моделью в кадре физически не укладываются в три минуты** — заявленный
бюджет реалистичен только если сцена 2 показывается по заранее сохранённой трассе/записи (как и
предписывает пункт 5: «живой вызов занимает минуты… для защиты использовать сохранённую трассу»), а
не вызывается заново на публике. Сцену «риск качества, прогноз, альтернативы» на настоящем snapshot
24.07.2026 в эту сессию не прогонял — нужно отдельно. Само хронометрированное прочтение вслух и решение,
что показывать живьём, а что по записи — шаг пользователя.

- У Against61 — простоту «одна команда → результат» и заметные предупреждения на первом экране.
- У решений с трассами — удобство проверки обмена ролей, но показывать у нас именно влияние на решение.
- У мониторинговых интерфейсов — быстрый доступ к ответу оператору, не превращая проект в новый dashboard.

Публичного работающего интерфейса большинства конкурентов не было, поэтому выводы об их визуальном
качестве по одному стеку или концептуальным изображениям не считаются подтверждёнными.

## Стоп-условие

После выполнения пунктов 1–4 и успешной трёхсценарной репетиции новые функциональные изменения не
начинаются. Пункты 5–7 завершают доказательства и упаковку. После них допускаются только исправления,
которые блокируют запуск, искажают результат или мешают показать обязательный сценарий ТЗ.
