# Самостоятельная перепроверка T39–T44

Проверен commit 8a1277d, без субагентов. Проверка не меняла исходники.

Подтверждено:
- Полный прогон: 784 passed in 46.90s, без предупреждений.
- AST-границы слоёв соблюдены, composition остаётся внешней сборкой.
- JSON четырёх основных сценариев и pause/resume проходят прежние проверки.
- Все Python-исходники финального архива совпадают с main.
- Fingerprint model.pkl совпадает с текущими исходниками и конфигурацией.
- Коммиты от текущего Git-пользователя без AI-соавторов.

Найдено, не исправлено в рамках проверки:

1. **P1: отказ snapshot не гарантирует отказ решения.**
   GetLiveAdvice.execute, строки 42–45: при trust.usable=False вызывает MakeDecision,
   который повторно оценивает state и может вернуть hold. Воспроизведено через fake HTTP client:
   snapshot с healthy state и trust.usable=False дал decision.status=hold без запуска forecast.
   Так возможно противоречие между внешним verdict и повторной оценкой, особенно при неполном
   trust_config. Нужно гарантировать отсутствие положительного совета при отказе snapshot.

2. **P2: нарушена совместимость /v1/live/advice.**
   DecisionService.live теперь возвращает LiveAdviceResult.to_dict, который не содержит
   inventories и sources. До T41 эти поля возвращались через _decision; проверено сравнением
   с commit 3293abf и фактическим ответом fake HTTP клиента. Нужно восстановить поля в транспортном
   представлении, не дублируя orchestration.

3. **P2: потерян структурированный отказ локального live при ошибке связывания.**
   LiveAdviceAdapter.advise теперь пропускает LiveError наружу из binder. До T41 возвращались
   decision=None, explanation=None, error и note. Воспроизведено прогнозом value=8, lower=7,
   upper=6, available=True: вместо прежнего JSON-отказа выброшен LiveError. CLI завершится
   до записи JSON/HTML отказа. Нужен прикладной контракт ошибки связывания и сохранение поведения.

4. **P3: финальный архив содержит устаревший контекст.**
   context/state.md и context/backlog.md в neftecode-20260914-194436.tar.gz отличаются от main:
   архив собран до последнего amend с результатом чистого теста. Код архива актуален.
   После исправлений нужно пересобрать комплект после фиксации документации.

Вывод: архитектурный перенос работает, но T41 и финальная упаковка T43 требуют доработки.
Зелёный текущий набор не доказывает полную совместимость live API и всех отказных веток.

## Исправлено после проверки

Все четыре пункта закрыты. Изменения, регрессионные тесты и чистая проверка описаны
в `live-fixes-2026-09-14.md`. Исторические воспроизведения выше относятся к 8a1277d.
