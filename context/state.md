# Состояние проекта

Обновлено: 2026-09-20. Этот файл — снимок текущего состояния, не журнал сессий. История задач и
промежуточных разборов удалена 2026-09-20 как устаревший рабочий мусор; что именно и почему —
`context/README.md`. Полная идея решения — `solution.md`, архитектура — `architecture.md`.

По запросу 2026-09-20 полностью просмотрен Telegram-чат «Хакатон «Нефтекод» МАС 2026» (586
сообщений, синхронизация до 2026-09-20). Организаторы уточнили состав сдачи: единый архив на
itmo.hack@itmo.ru, тема письма — название команды; внутри код, презентация/README с описанием или
дашборд, дополнительные файлы опциональны. Код должен быть понятен для запуска и воспроизведения.
Дедлайн — 23:59 22.09.2026 МСК. Для отборочного этапа презентация или текстовый файл допустимы на
выбор; презентация обязательна уже на финальной защите для прошедших команд.
Сводка содержательных уточнений организаторов и QA-сессий сохранена в
`context/organizer-clarifications.md`; это дополнение к ТЗ, а не новые требования сверх подтверждённых
ответов.

Текущий режим работы — завершение и упаковка, а не расширение функционального ядра. После сравнения с
публичными конкурентами и визуальной проверки нового интерфейса утверждена конечная очередь задач в
`context/finalization-plan.md`: сначала исправить противоречивые статусы и режимы LLM, затем поднять
ответ оператору над подробной схемой, убрать внутренние коды из основного слоя и проверить сдаваемый
архив одной командой; после этого — доказательства мультиагентности и прогноза, трёхминутная репетиция.
Новые агенты, модели, чат и большой dashboard до сдачи не планируются.

## Что работает

Текущий фокус пользователя (2026-09-20): сначала разбирать именно исходное ТЗ, не подменяя его описанием
нашей реализации; затем последовательно закрывать замечания к решению относительно ТЗ. Полный текст
`task/ТЗ_нефтекод.docx` повторно прочитан по всем 5 страницам. Обязательное ядро ТЗ: целостная МАС для
рекомендации оператору по цепочке АВТ → гидроочистка → блендинг; временно корректная работа с данными;
прогноз качества и риска; оценка тяжести режима; несколько вариантов; жёсткий Gate; объяснимый выбор либо
обоснованный отказ; воспроизводимый Python-запуск и проверяемая трасса решения. Таблица конкретных ролей —
пример, а неопределённость, детектор аномалий, dashboard, Парето-фронт и дополнительные агенты прямо
помечены как необязательные. Изменения должны сохранять явные границы прототипа и не выдавать сценарные
проверки за промышленное доказательство.

Советчик оператору АВТ → гидроочистка → смешение: доступные данные → проверка доверия → прогноз →
постоянные/переходные планы → модель последствий и запасов → обязательные проверки (Gate) → сравнение →
объяснение. Поверх детерминированного контура — слой LLM-агентов (`OrchestratorAgent`, `QualityAgent`,
`ReliabilityAgent`): выбирают инструменты и предлагают ужесточающие ограничения; числа, допустимость,
ранжирование, финальная перепроверка и устойчивость — тот же детерминированный код, любой сбой LLM —
детерминированное решение без изменений. Четыре HTTP-процесса (data, model, decision, gateway) — общее
ядро; запуск — `neftecode-stack`. CLI: `train`, `advise`, `snapshot`, `screen`, `scenes`, `serve`,
`benchmark`, `agent-demo` и др. — `neftecode --help`.

Backend: **1359 тестов проходят** (последний полный прогон 2026-09-20, после перезаморозки эталонов).
Frontend: `npm run build` (tsc + vite) — без ошибок.

## Явные границы — не готово к промышленному внедрению

Это не забытые пробелы, а объявленные условия — код сам их проверяет и показывает в каждом решении:

- **`deployment_readiness.ready = false`.** Завод не передал фактический рабочий объём и полезную
  вместимость резервуарного парка и допустимую мощность наработки ДТ более глубокой очистки. Пока это
  не получено — числа парка (4000 т и т.п.) остаются только сценарными.
- **`commercial_release_allowed = false`** всегда, по конструкции. Советчик не разрешает товарный выпуск.
- **Прогноз серы после ГО**: цель `exceed_upper` ≤5% на честном слепом тесте 2026 года **не достигнута**
  (фактически 6.01%). Улучшение MAE (1.802 → 1.616 мг/кг) подтверждено, целевой порог — нет; это не
  подгонялось задним числом.
- **Польза управления, ресурс оборудования и промышленная безопасность прямо не доказаны** — отдельно
  зафиксировано по итогам Q&A с организаторами; это дополнительная метрика/диагностика, не доказательство.
- **Смещение ПАК–ЛИМС в оценке серы резервуара** — исследовано, само по себе непостоянно (медиана по
  полугодиям от −0.97 до +1.04), варианты учёта предложены, но не внедрены — ждёт отдельного решения.
- Ряд пунктов ТЗ сознательно не реализован с объяснением почему (не пропущен): ВАК не служит источником
  качества (формулы проверены и как измерение непригодны), соотношение ВСГ/сырьё не моделируется как
  управляющая переменная (нет подтверждённых границ и отклика), давления АВТ/ГО — не управляемые
  воздействия, Парето-фронт по нескольким целям не строится (завод не задал веса). Подробности и
  обоснования — `README.md` (корень, раздел ограничений) и `requirements-map.md`.

## Последние изменения (2026-09-19 — 2026-09-20)

6. **Сохранение агентных ограничений при сбое (2026-09-20):** если оркестратор завершается с ошибкой
   или без финального ответа после подтверждённого veto/ограничения, решение больше не возвращается
   поверх этих ограничений. Выбор идёт только среди `allowed_ids()`, при пустом наборе возвращается
   отказ, выбранный план проходит повторный `_guard` на свежем экземпляре того же планировщика. Добавлен регрессионный тест
   `test_orchestrator_failure_preserves_confirmed_veto`; два focused-теста проходят.
7. **Надёжность участвует в выборе (2026-09-20):** добавлено явное сценарное окно
   `severity_cost_tolerance_fraction`. При одинаковом выпуске и небольшой разрешённой разнице стоимости
   выбирается меньшая тяжесть режима или меньшее число действий; за пределами окна сохраняется прежний порядок.
   Механизм включён в `sour_crude`, цена компромисса записывается в решение.
8. **Совместный стресс отклика (2026-09-20):** `RobustnessCheck` проверяет одновременные слабый отклик
   гидроочистки и увеличенную задержку (`COMBINED_RESPONSE_STRESS`). Он обязателен для планов с ходом ГО и
   неприменим к `hold` или чистому изменению смеси.
9. **Закрытие критических дефектов выбора (2026-09-20):** неверный `select` оркестратора после veto больше
   не возвращает запрещённый legacy-план; восстановление выбирает только из `allowed_ids()` или отказывает.
   Окно надёжности согласовано с `min_useful_gain`, цена компромисса записывается в `selection_policy`.
10. **Обязательная устойчивость и независимая среда (2026-09-20):** обязательные возмущения теперь исключают
    хрупкий план и запускают повторный выбор. В поставляемый набор входят совместный стресс отклика/задержки и
    верхняя оценка серы основного компонента. Добавлена независимая сценарная оценка стратегий с фиксированными
    альтернативными откликом, задержкой и свойствами смешения (`evaluation/independent.py`).
11. **Сценарная политика надёжности:** `sour_crude` включает окно стоимости 3% и предел прокси-тяжести 0.75;
    это сценарные параметры, не заводские нормативы. Полный backend-набор: 1359 тестов проходит; frontend
    собирается `tsc + vite`; шесть демонстрационных сцен и `agent-demo` проходят без внешнего LLM.
12. **Обязательный совместный стресс отклика во всех сценариях (2026-09-20):** `COMBINED_RESPONSE_STRESS`
    входит в `DEFAULT_PERTURBATIONS` с `mandatory=True`, а не только в policy `sour_crude`. Из-за этого
    изменились `decision_id` и sha256 замороженных решений `baseline` и `ample_reserve` (статусы и выпуск
    те же) — эталоны в `tests/architecture/test_architecture_baseline.py` и `tests/agentic/test_legacy_baseline.py`
    переморожены; `tests/domain/test_response_guard.py` проверяет guard на диагностическом наборе возмущений,
    чтобы обязательный стресс не отсекал план «на лезвии» раньше guard'а.
13. **Документы приведены к `benchmark.json` и `scenes.json` (2026-09-20):** README и `solution.md` больше
    не содержат 180 / 240 т, «15 нарушений у порогового правила» и план `c0041`; аргумент «зачем сложнее
    порогового правила» переписан честно — на поставляемом наборе бенчмарк превосходства не показывает,
    отличие видно в независимой оценке. CFPP: код `F65/(F32+F30)` соответствует официальному файлу
    `формулы_ВАК.xlsx` от 16.09; `requirements-map.md` и `organizer-clarifications.md` исправлены (раньше
    ссылались на отменённый ответ 10.09).
12. **Ходы АВТ в live:** `bind_measurements` отключает предложения по расходу сырья и температуре печи АВТ.
    Сценарная задержка 0.5 ч не переносится в live как обещание быстрого эффекта без подтверждённого времени
    прохождения промежуточных ёмкостей и измеренного отклика товарного качества.

1. **Баг explain-слоя**: `refusal.kind == "agent_rejected"` (допустимый план был, но агенты качества/
   надёжности его отклонили) молча схлопывался в `no_feasible_plan` — экран лгал «допустимых вариантов
   ноль», хотя Gate реально находил допустимые кандидаты. Исправлено в `explain_types.py`, `refusal.py`,
   `risk_block.py`, фронт (`ChoiceStage`, `GateStage`, `Refusal.tsx`). Слито с параллельным чужим коммитом
   (`mikhailkhorokhorin`, рефакторинг фронта) через `git merge`, конфликт был только в собранном
   `dist/assets/index.js`, решён пересборкой.
2. **Ускорение агентного слоя**: независимые tool-вызовы одного ответа LLM (в первую очередь
   `ask_quality_agent`/`ask_reliability_agent`, которые оркестратор реально просит в одном шаге)
   выполняются параллельно (`ThreadPoolExecutor` в `agentic/loop.py`), кроме `search_candidates`
   (мутирует session) и `finalize`/`submit_opinion`. Добавлены блокировки для общего состояния
   (`AgentBudget`, `DecisionSession.veto`, `AgentTrace.add`); прогресс-события явно прокинуты в рабочие
   потоки. Измеренное ускорение — **×1.6–2×** (не ×4: глубина диалога оркестратора с LLM, 2–5
   последовательных раундов, принципиально не параллелится без смены протокола).
3. **Найден и исправлен недетерминизм вердикта**: `LLM_TEMPERATURE=0.2` по умолчанию давал на одном и том
   же пограничном входе (запас по сере 0.39 против операционных 1.0) то `confirmed_legacy`, то `refused`.
   Дефолт изменён на `temperature=0` (`infrastructure/llm/config.py`) — два контрольных прогона подряд
   дали идентичный вердикт и время (разброс 0.5% вместо ~33%).
4. **Широкая проверка обеих правок**: все 6 синтетических сценариев (`ample_reserve`, `baseline`,
   `light_component`, `no_feasible`, `sour_crude`, `winter_grade`) — без падений, `invalid_final: 0`
   везде (нет признаков вырождения ответов от `temperature=0`); реальный исторический момент
   (`advise --at 2026-05-07T10:10:00`, тот самый со специалистами) — 105.7с против исторических ≈212с,
   тот же вердикт; прод-путь через реальный HTTP-стек (`neftecode-stack` → `gateway`/`decision_service`,
   `/api/decide`) — 99.7с, без ошибок, кэш гейтвея подтверждён (повтор 0.013с, тот же `decision_id`).
5. **Чистка репозитория (2026-09-20)**: удалены ~40 файлов рабочего журнала (`context/backlog.md`,
   `tasks-*.md`, `qa-session-*.md`, разовые аналитические записки, `plan/00–08`, корневые `TODO.md`/
   `FRONTEND_TODO.md`, `context/history/`) — история задач и промежуточных обсуждений, не описание
   решения. Мёртвые ссылки на них вычищены из оставшихся документов (`README.md`, `architecture.md`,
   `solution.md`, `defence.md`, `requirements-map.md`, `data-flow.html`) и из строки кода
   (`infrastructure/data/quality.py`). `context/benchmark-audit-2026-09-19.json` по ошибке попал под
   удаление и восстановлен — на него ссылается `tests/evaluation/test_benchmark.py`.

## Прогресс по finalization-plan.md (сессия в worktree, 2026-09-20)

Ветка `worktree-finalization-plan`, от `a728a10`. Ядро (`src/neftecode`) не менялось — только фронтенд.

Пункты 1–4 (все обязательные) сделаны в коде и проверены живыми данными через локальный
`neftecode-stack`, поднятый в worktree на read-only копии `artifacts/` (4,2 МБ, скопирована из
основного чекаута) и symlink на `task/` — основной чекаут и живая сессия пользователя (порты 8799/
8765) не трогались. Пиксельная проверка в браузере не выполнена: в этой фоновой сессии нет
графического Chrome/расширения claude-in-chrome — остаётся на репетицию (пункт 7) на машине
пользователя.

- **Пункт 1 (статусы этапов) — закрыт и проверен вживую.** Найдена и исправлена реальная причина
  противоречия «сработал запасной путь» / «отката не было»: backend (`decision.py`) никогда не шлёт
  `agentic.outcome == "ok"` (реальные значения — `selected`/`confirmed_legacy`/`refused`/`fallback`/
  `skipped`) и никогда `mode == "scripted"` — фронт сравнивал именно с этими значениями. Три живых
  `/api/decide`: без ключа провайдера → `outcome="fallback"` с причиной (не противоречит); scripted →
  `outcome="confirmed_legacy"`, `deterministic_policy=true` — раньше это и было противоречие, теперь
  тон «scripted»; отказ на данных (`fault=both_broken`) → `outcome="skipped"`. Последний случай вскрыл
  ещё один баг: этап «Агенты» тоже жёстко помечался «готово» даже когда агенты не привлекались вовсе —
  исправлено (`LATE_STAGES` теперь включает `agents`, `stageRan` проверяет `agentic.outcome`).
- **Пункт 2 (ответ оператору над схемой) — сделан.** Новый `ui/OperatorAnswer.tsx` рендерится в
  `App.tsx` до `<PipelineMap>`: вердикт → что сделать относительно текущего режима → причина →
  обязательное предупреждение (приоритет — `fragile_plan`/`tank_estimate_sensitive` из
  `explanation.risk.items`, стилизовано заметнее идентификатора плана). Логика прогнана на трёх живых
  ответах; ветки risk-warning и deployment_readiness сработали как задумано, реального
  `fragile_plan` в доступных сценариях не встретилось.
- **Пункт 3 (внутренние коды) — закрыт.** `legacy_status`, `outcome`, `scenario_id` на основном
  экране получили человеческие подписи (коды остаются в JSON/`<code>` рядом). Добавлена короткая
  цепочка «ядро предложило → специалисты проверили → ограничения/вето → итог» (`AgenticMode.tsx:
  AgentChain`), проверена на живом scripted-прогоне — текст совпал с реальным payload.
- **Пункт 4 (поставка одной командой) — закрыт.** С нуля: `uv sync --frozen` → `uv run pytest -q` —
  1357 passed, 2 skipped (1359, как и раньше); `uv run neftecode scenes` без срезов — 5 сцен, оба
  предупреждения как в README. `bash scripts/package.sh` прогнан целиком (артефакты — read-only
  копия из основного чекаута, обучать заново не понадобилось): все обязательные файлы на месте,
  опциональный `expert_grid.json` пропущен с предупреждением, а не ошибкой; собран
  `neftecode-20260920-154407.tar.gz`, 2,2 МБ, 626 файлов, без `task/`, встроенные проверки на секреты
  и обращения к внешней сети прошли.
- **Пункт 5 (доказательство мультиагентности) — первая часть подтверждена, вторая блокирована.**
  `artifacts/agent-live-specialists-20260917.json` реально содержит закрытый живой прогон
  (`zai/glm-5.3-flash`, 7 `llm_call`, 34 831 токен, `outcome=confirmed_legacy`, без отката) — уже
  входит в собираемый `package.sh`-архив. Второй живой прогон (сценарий с реальным вето) не сделан:
  в среде нет `ZAI_API_KEY` — новый вызов Z.AI отсюда невозможен физически, не по выбору.
- **Пункт 6 (доказательство прогноза) — сделан.** Новый `context/forecast-evidence.md`: ориентир →
  прогноз → пять rolling-полугодий → честный финальный тест 2026 (`exceed_upper` 6.01% — цель 5% не
  достигнута, сказано прямо) → пять пунктов ограничений → реальный момент 2026-01-05, где измерение
  вместо сценарной уставки вдвое сократило окно до нарушения. Числа не пересчитаны — взяты из уже
  проверенных README.md/RESPONSE_MODEL_T6.md.
- **Пункт 7 (репетиция) — не начат.** Структурно требует человека: хронометраж вслух, реальный клик
  по интерфейсу в браузере на машине пользователя. Здесь нет ни того, ни другого — `claude-in-chrome`
  не подключён (`list_connected_browsers` пуст), таймер репетиции не заменить.

Итоговая проверка после всех правок этой сессии: `bash scripts/package.sh` — 627 файлов (было 626 до
`forecast-evidence.md`), 2,2 МБ, все встроенные проверки прошли.

`npm run build` (tsc --noEmit && vite) проходит после каждого шага. Восемь коммитов в worktree —
не запушены и не слиты в `main` (проверено: `origin/main` фактически уже на `a728a10`, а не на
`db0b252`, как ошибочно было написано в предыдущей версии этого раздела).

## Git

Последний запушенный коммит в `origin/main`: `a728a10` (слияние: схема расчёта + чистка документации).
Уточнено 2026-09-20 через `git fetch`; предыдущая запись про `db0b252` устарела.
