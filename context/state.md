# Состояние проекта

Обновлено: 2026-09-14. Ветка main. Перенос всего пакета на слоистую Clean Architecture
с DDD-подходом. Каждый этап выполняется отдельным коммитом от kaifarikman, без AI-соавторов,
и проверяется независимым субагентом GPT-5.6 Luna medium. `main` отправлен в `origin/main`
до коммита `adaeef2` по прямому поручению пользователя; отправка организаторам не выполнялась.

## Текущая задача

Начата эталонная очистка T39–T44. Независимый аудит подтвердил два главных дефекта структуры:
старый `infrastructure.ml.agents.Coordinator` дублирует основной `MakeDecision`, а `bootstrap.py`
содержит обработчики всех команд. Дополнительно найдено изменяемое состояние запроса
`_current_operation` внутри `MakeDecision`. Публичные CLI/HTTP/JSON-контракты основных сценариев
сохраняются; старые внутренние demo-артефакты отдельным контрактом не считаются.

T39 завершена: `MakeDecision` не хранит `current_operation` между запросами и явно передаёт её
в планировщик, финальную проверку, robustness и JSON. Полный прогон — 784 теста, хеши основных
решений прежние. T40 удаляет старый одношаговый Coordinator и переводит demo на `MakeDecision`.

T40 завершена: единственный центр принятия решений — `MakeDecision`. Старый Coordinator,
одношаговый конфиг, legacy runtime/claims и неинтегрированные support/twins/attribution удалены.
Demo, replay и train-артефакты переведены на основной контур; исследовательский risk остаётся
отдельной метрикой и не может отменять Gate. T41 завершена: локальный CLI и decision-service
собирают live-рекомендацию только через `GetLiveAdvice` и типизированные порты. Полный прогон —
782 теста; реальный live-контракт сохранил 5.88/9.17 и `hold`. T42 завершена: bootstrap сокращён до 23 строк; сборка перенесена в `composition/`, команды —
в `composition/commands/`, Markdown-отчёт — в `presentation/reports/`. 782 прежних теста и
2 новые архитектурные проверки проходят; Luna-аудит пройден. T43 завершена: границы и документация закреплены, модель переобучена,
чистый комплект проверен (783 passed, 1 ожидаемый skip без task/). T44: устранение предупреждений.

T35–T38 завершены: поверх Clean Architecture проект работает как набор отдельных
локальных HTTP-процессов — data, model, decision и gateway. Общими остаются доменные типы и
версионированные JSON-контракты. Прежние CLI-команды и браузерный JSON сохранены.

Общая HTTP-основа уже реализована: envelope `v1`, request ID, единые ошибки, лимиты JSON,
межсервисный timeout и `healthz`/`readyz`. Data-service выдаёт сценарии и подписанный по содержимому
snapshot; model-service проверяет его время/схему/хеш и единственный загружает bundle. Реальный
snapshot→forecast сохранил 5.88 мг/кг и верхнюю границу 9.17. После T36 проходит 769 тестов.

Decision-service принимает сценарное решение сам и собирает live advice через data/model HTTP.
Gateway-service только преобразует браузерные запросы и рисует экран. Сквозной HTTP-тест сохраняет
decision ID локального режима; полный прогон после T37 — 776 тестов.

`neftecode-stack` запускает четыре subprocess, ждёт health/readiness и завершает всю группу при
сбое или сигнале. Все процессы также имеют отдельные команды. Полный прогон: 783 теста.
Модель переобучена: fingerprint `c77035c513fbe4b32654e3229698d2784b15b1682e7c6503f106b87686c6f299`,
выбраны `last_pak` для серы и `last_lab` для T95; advise сохранил 5.88/9.17 и `hold`.

Этапы T28–T34 завершены. Домен разнесён по предметным контекстам. Прикладное ядро выражено
через `MakeDecision`, `PlanOperation`, `GetLiveAdvice`, `ReplayDecisions`, типизированные команды
и порты. CSV/XLSX, JSON-сценарии, ML, live-доступ и сохранение JSON вынесены в
`infrastructure`; старые плоские импорты этих модулей удалены. CLI, demo, HTTP и HTML находятся
в `presentation`, а сборка адаптеров — в `bootstrap.py`. Benchmark, ВАК, эпизоды, лаги,
margin и устойчивость вынесены в `evaluation`; файловое чтение ВАК осталось в infrastructure.
Все 783 теста проходят, JSON-хеши совместимых сценариев совпадают, независимые аудиты Luna medium пройдены.
Модели переобучены после смены путей, итоговый комплект проверен в чистом окружении.

## Итог

T01–T27 завершены; повторно открытые T08/T12/T15/T18/T21/T23/T25 исправлены. Аудит A1–A7 закрыт.
T22/T24 пересчитаны, T26 проверена в чистом окружении. Материалы защиты T27 актуализированы.
Резервные R01–R03 не начинались. T28–T38 завершили Clean Architecture и запуск четырёх
отдельных процессов. Итоговый архив собирается локально в `dist/`, вне Git.

Последняя проверка T43: **784 passed**, 228 предупреждений. В чистом архиве без `task/`: 783 passed и один
ожидаемый skip реального live-контракта. `uv sync --frozen`, scenes, train, advise, benchmark,
старый браузер и стек отдельных процессов проверены. Полная идея —
solution.md, архитектура — architecture.md, задачи и история
результатов — backlog.md, защита — defence.md, исправления — completion-2026-09-12.md.

## Что работает

Советчик оператору АВТ → гидроочистка → смешение: доступные данные → проверка доверия → прогноз →
постоянные/переходные планы → модель последствий и запасов → обязательные проверки → сравнение →
объяснение. Агенты — программные роли внутри decision-контекста; CLI и четыре сервисных процесса
используют одно ядро. Нет автоматического управления заводом; commercial_release_allowed=false.

- Сера и T95 проверены на истории; простой прогноз не побеждён. Для цетана 42 анализа, ML не заявлен.
- Сера, T95 и цетан смеси проверяются по каждой точке. Неизвестный предел T95 не наследует серный.
- Приток постепенно меняет массу и свойства запаса, а не мгновенно весь резервуар. Расход
  притока сценарный, внутри шага сначала отбор, затем смешение; T95/цетан — линейное приближение.
- Gate требует полные уставки, запасы, сетку и конечный остаток. Запрет агента исключает план.
- Feedback создаёт новые варианты; бюджет ограничен, одинаковое содержимое пропускается.
- Replay учитывает фактические остатки, рецепт, выпуск, дозу и время подтверждённых действий.
  Сохранение/продолжение воспроизводимо. Snapshot без журнала считается установившимся режимом.
- Экран показывает рецепт и управление до/после. Плохие данные останавливают прогноз заранее.

## Проверенные результаты

`artifacts/benchmark.json`: четыре прежних сценария, 5 записей выигрыша и 2 проигрыша по метрикам.
В sour_crude 180 т без нарушений; без переходных планов тоже 180 т; без конечного резерва 240 т.
**Прежний выигрыш переходного планирования не подтвердился**, старые 240/180/300 т не использовать.
На baseline пороговое правило дешевле (1.05 против 1.09) и расходует меньше резерва (0 против 30 т).
На ample_reserve обе стратегии дают 300 т с одним изменением. Это сценарные результаты, не экономия завода.

`train`: 189217 строк телеметрии, 1458 целевых анализов серы, 296 признаков. Выбран last_pak;
T95 — last_lab. Сера validation: 1.3241 против 1.3205 у CatBoost, отрыв 0.27% несущественен.
T95: 4.849 против 8.148. `advise` 2026-01-05 08:00: 5.88, верхняя граница 9.17 мг/кг, hold.
Тест 2026 уже просмотрен и не является новым слепым тестом.

## Предметные границы

Полное ТЗ и уточнения сохранены в task-review/ и organizer-clarifications.md. Синтетический
блендинг и собственная обоснованная модель отклика разрешены экспертом. Горизонт 0–3 ч,
пересчёт 15–60 минут, ЛИМС доступен до 4 ч после отбора (точных дат выдачи нет).
Сера продукта ≤10 мг/кг; T95/цетан, запасы, цены, лаги и эффект присадки заданы сценарием.
Масштабы P8/T11/F19 и Pipeline остаются открытыми. ВАК/корреляция не доказывают причинность.
Ресурс оборудования, промышленная безопасность и польза управления не доказаны.

## Следующее действие

Архитектурный перенос завершён. Резервные исследования R01–R03 не входят в этот перенос.
Telegram повторно полностью прочитан до сообщения 552: новых технических ответов нет, запись Q&A
и обновлённые схемы не опубликованы. Подробности — `organizer-clarifications.md`. Следить за их
появлением; отправка организаторам не выполнялась. Для разбора реализации добавлена пошаговая
карта `code-tour.md`. Следующий этап — T44: убрать предупреждения временных расчётов без изменения чисел.
